"""M4 -- sync-committee update verifier. See docs/design/004-sync-committee.md.

Deployed contract: `contracts.sync_committee.verifier.SyncCommitteeVerifier`.
Supporting modules (compile-time subroutine libraries, imported into the
verifier, not deployed separately): `constants`, `forks`, `header`,
`bitfield`, `install`.
"""
