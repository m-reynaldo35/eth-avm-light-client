---
name: Bug report
about: Something in a contract, the relayer, or the live service behaves incorrectly
title: ""
labels: bug
assignees: ""
---

**What happened, and what did you expect instead?**

**Which module/file** (e.g. `contracts/receipt/`, `relayer/client.py`,
`service/x402_endpoint/`)?

**How to reproduce** — a real command, transaction id, or app id, if
possible. This project treats "real, measured evidence" as the standard for
any claim (see `ARCHITECTURE.md`) — the more concrete, the faster this gets
fixed.

**Environment**: network (mainnet/testnet/dev-mode algod), `puyapy`/Python
version if relevant.
