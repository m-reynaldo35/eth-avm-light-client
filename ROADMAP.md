# Roadmap

This file is the single source of truth for "what's next." Read this first in
any new session before doing anything else.

**Workflow for every module**: an Opus design pass writes `docs/design/NNN-*.md`
and commits it → human reviews and approves the design doc → a Sonnet
implementation pass writes code + tests against it, and commits → this table
gets updated. Implementation never starts on an unapproved design doc.

**Legend**: Not Started / Design Drafted / Design Approved / Implementing / Implemented / Tested / Released

| # | Module | Status | Design doc | Depends on | Open questions inherited from spike | Last updated | Next session should |
|---|---|---|---|---|---|---|---|
| — | Scaffold | Implemented | — | — | — | 2026-07-30 | Push scaffold to GitHub, then start M1 design |
| M1 | BLS12-381 point codec & MSM/pairing wrapper | Implemented | [docs/design/001-bls-primitives.md](docs/design/001-bls-primitives.md) | scaffold | Resolved: trust boundary = subgroup-check + recompress-and-compare against SSZ-committed compressed bytes (bare subgroup check is provably insufficient, see doc §4.4 attack trace). **P1 now measured**: `ec_map_to BLS12_381g2` = 8185 (confirms the doc's 8150 citation). P3 spot-measured (g2 subgroup-check 2373, g2 add 338). P9 spot-check at small n confirms ec_add-chain glue is under the 45/iter threshold — chain stays correct default. | 2026-07-30 | 52/52 tests pass incl. live algod (contracts/primitives/bls/, tests/bls/). Gaps before "Tested": no `tests/bls/generate_fixtures.py` / persisted `tests/fixtures/bls/*.json` (design doc checklist item 8 requires this); P4-P8, P10-P11 unmeasured; P9 not measured at the real n=42 chunk size (hit the 2048B app-arg cap as design doc §7.3 predicts — needs the box-staging path). Not yet committed — awaiting human review. |
| M2 | On-chain optimized RLP decoder | Implemented, gates 4/6 (G2, G4, G5, G6) | [docs/design/002-rlp-decoder.md](docs/design/002-rlp-decoder.md) §16 | scaffold | **O-1/O-2 pulled forward per human direction, 2026-07-31 (§16)**: raw-TEAL injection investigated and found not cleanly achievable in the installed Puya/algorand-python toolchain without abandoning single-source-of-truth (a real, useful side-finding: `op.Scratch` with a compile-time-constant slot index DOES compile to cheap immediate-form `store`/`load`, so genuine manual scratch is possible within Puya — measured to bring no net win for this loop, so not used). O-2 (general table-free capture) was implemented and measured to bring **no net benefit** for a full 17-item walk (removed table-write cost is offset by added want-index compare cost — both ~equally dominated by Puya's fixed per-item control-flow cost, ~30-35 opcodes/item regardless). O-2 specialised to the always-exactly-2-item MPT ext/leaf case (`rlp_scan2`, no loop at all) and O-1 early-exit (`rlp_scan_upto`, walks only to the wanted index) were both shipped. **G6 (the gate that matters — real 8-node account-proof composition) now measures 2,566, beating the spike's 3,276 by ~22%** (was 5,302 pre-§16). G3 improved 291→192 (still misses the ≤90 target). G1 no longer has one clean number (cost is now index-dependent by design at the caller's choice between `rlp_scan_upto`/`rlp_scan2` and the unchanged flat-cost `rlp_scan`/`rlp_table_item`) — both are reported honestly in `bench/rlp_results.json` rather than forced to pass. G2/G4/G5 unaffected/still pass. `rlp_scan`/`rlp_table_item`/`mpt_node_scan` are completely unchanged and remain correct for repeated-access or full-arity-check callers. | 2026-07-31 | M5 design should use `rlp_scan2`/`rlp_scan_upto` (§16) as the default MPT-node descent entry points, falling back to `rlp_scan`/`rlp_table_item` only if a hop genuinely needs repeated access to the same node or a full arity check. |
| M3 | SSZ Merkle branch verifier | Implemented | [docs/design/003-ssz-verifier.md](docs/design/003-ssz-verifier.md) | scaffold | Resolved: sha256 measured at flat 35 budget (3.71x cheaper than keccak256); exact cost formula `53+61*depth+2*z` validated against 38/38 official consensus-spec test vectors across 6 forks (Altair-Gloas); fork decision = gindex is a runtime parameter, never hardcoded (spec itself is fork-parameterized at runtime). **NEW finding from implementation**: real Puya-compiled cost is ~40% higher than the design doc's hand-TEAL numbers (~`103+83*depth` vs `53+61*depth+2z`) — single-call depth ceiling is actually ~7-8, not 10. **M4 must use this revised ceiling, not the design doc's**, when sizing sync-committee/finality proof calls. | 2026-07-30 | 165/165 tests pass (contracts/primitives/ssz/, tests/ssz/). Gaps before "Tested": T4 (independent hash_tree_root check via remerkleable/pyspec) not done; full-scale 512-pubkey/99-call T13 not run live (only small-scale n=5 confirmed); ci-live.yml/ci-offline.yml still placeholders (M11's job). Not yet committed — awaiting human review. |
| M4 | Sync-committee update verifier | Implemented, install-flow box-cap gap fixed and verified live | [docs/design/004-sync-committee.md](docs/design/004-sync-committee.md) §16 | M1, M3 | **Design-level findings confirmed correct in real code**: signing-root/DST construction (§3), `fork_version` at `signature_slot-1` trap, 28-byte domain truncation, and SSZ Bitvector[512] bit-order remap (new T5, 6/6 tests, proven via exhaustive round-trip) all verified against real vendored vectors. Also found and fixed a **test-harness bug** (not a contract bug) in the original committee-rollover tracking, requiring a hand-trace against real `consensus-specs@v1.6.0-beta.0` pseudocode — documented a minor labeling erratum in design doc §3.4's table (3 cells mislabeled current-vs-next committee; does not change any signing-root/BLS-result value). **§16, this pass: the install-flow box-cap gap is FIXED and verified live.** Measured (not assumed) against real dev-mode algod: the box-reference cap is exactly 8 per TRANSACTION (structural, confirmed via the literal protocol error), and there is a SEPARATE, real, per-box-reference budget of 2,048 B, POOLED ACROSS THE WHOLE ATOMIC GROUP (not the 1,024 B/ref the design doc originally cited, and not per-transaction) — both measured directly, several ways. `bootstrap`/`install_begin` are now validation-only (`INST_STATE_VALIDATED`, touching at most the `forks` box — a THIRD independent hit of the same cap was found and fixed this pass, since bootstrap's own `forks` read plus 8 key-box creates was also 9 refs in one call); two new ARC-4 methods, `install_open_keys` (`OPENING_BOXES`) then `install_open_session` (`INSTALLING`), split box creation across sibling transactions in the same atomic group, padded with `noop_budget()` calls to clear the pooled-budget floor (25 total refs measured minimum). Atomicity (not just the `inst_state` guard) is what prevents any partially-open box set from ever being observable on-chain. **Group count is UNCHANGED at 8** (§8.5) — the fix adds transactions within the existing first group's 16-slot budget, not new groups. **Live-tested end-to-end against real algod**: `tests/sync_committee/test_install_live.py`, 4/4 passing — the box-opening group actually commits (was blocked before), `install_chunk` proceeds correctly afterward, and the negative case (`install_chunk`/`install_open_session` called before box-opening completes) genuinely rejects. **Honest gap left open** (§16.5): a THIRD related but distinct finding — `box_extract`/`box_replace` on an existing box charges the pool the box's FULL declared size once per box per group, not the touched slice, so every `install_chunk` call needs >= 4 box refs (not 2) regardless of chunk size — is measured and documented but NOT re-derived across a full 512-member, 8-group install; only one small chunk was exercised live (via simulate, to isolate from the separate, pre-existing opcode-budget-donation mechanism). Does NOT affect §3/§5 correctness (signing root, bitfield, aggregation) — those remain solid. | 2026-07-31 | 284/284 tests pass project-wide (280 pre-existing + 4 new live install-flow tests), verified independently after every change in this pass. **Open follow-up for a future pass** (not blocking, not this task's scope): re-derive §8.5's box-reference bookkeeping for the full 512-member `install_chunk` sequence now that the full-box-size-per-touch charge is known, and confirm it live end-to-end (only a small chunk was tested here). Not yet committed — awaiting human review. |
| M5 | MPT path-walker / node verifier | Implemented, real-submission fix landed (§16), budget gates still open | [docs/design/005-mpt-walker.md](docs/design/005-mpt-walker.md) §16 | M2 | **Security fix resolved**: branch/extension/leaf descent all derive their index/comparison from the key itself on-chain (`nibble_at(key, depth)`), never from a caller argument — verified against all three real fixture proofs, with the account-proof indices matching M2's own G6 bench exactly. Leaf match requires `depth + n_path == key_nibs` **exactly** (not `<=`), which is what rejects a strict-prefix leaf. **Args-vs-box resolved**: raw app-args, segmented walk, one atomic group, no boxes; segment hand-off verified via `gtxn LastLog`, tested live for both honest-pass and forged-reject. **§16, this pass**: found and fixed two real, measured implementation inefficiencies flagged after the first implementation pass shipped ~2x over target — `mpt_descend` was redecoding the same node twice per branch hop (once in `mpt_walk_node` to learn arity, again inside `mpt_branch_hop`), and `W`'s 101-byte buffer was fully reconstructed by concatenation every hop instead of splicing only the ≤35 bytes that change. Fixing both recovered a REAL, measured 1,303 budget (20%) on the headline 8-node account walk (6,419 → **5,116**) and 383 (17%) on the 3-node receipt walk (2,196 → **1,813**), with zero change to §5's security logic (72/72 mpt unit tests + 350/350 project tests pass unchanged). **Inner-transaction budget donors wired up** (§7.6's named escape hatch, M1 §9.1 / M4 §16's pattern): `MptSegmentApp` now issues no-op inner app calls to a separate donor callee before doing real work. **Headline result: a REAL, non-simulated submission of the full 3-segment, 8-node account-inclusion-proof group now succeeds end-to-end** (6 donor calls), and the 2-segment receipt-proof group also succeeds (2 donor calls) — this was flatly impossible before this pass (a single segment failed outright with `dynamic cost budget exceeded`, not merely under-`simulate`d). **Honest gap left open**: G6-M5 (5,116 vs <3,276 target), G1-M5 (1,813 vs <1,121), and G5-M5 (1,969 B vs ≤1,400 B compiled size) still do not pass; §16.4 attributes the remaining ~2,550-budget gap to real Puya-compiled control-flow/subroutine cost across §5's genuinely larger (correctly key-bound) surface versus M2's G6 baseline, not to further double-work — no additional double-decode or redundant-copy waste was found. The design's own named lever (O-M5-1, shape-based discriminator) would close only ~4% of the remaining gap. | 2026-07-31 | Budget/size gates (G1/G5/G6-M5) remain open for a future optimization pass; §16.4 discusses the tradeoffs (revisiting §5.1's unconditional-discriminate architecture, or accepting the security-fixed walker's real per-hop cost). M6 may proceed against the security-verified, real-submission-capable walker as-is. |
| M6 | Account & storage proof composer | Design Approved | [docs/design/006-account-storage-proof.md](docs/design/006-account-storage-proof.md) | M5 | **Resolved**: exclusion at composite level answered for both cases (account absent -> trivial slot-absence, no phase B needed; account present but slot absent -> a real exclusion proof against the verified storageRoot). **Security-critical hand-off**: storageRoot is never an argument -- it's computed on-chain from the verified account body and passed via the same group-log-reading mechanism M5 established (`gtxn LastLog`), not trusted from a caller. Traces a concrete attack (relayer substitutes a different contract's storage proof under a fabricated composition) and shows it's defeated by the hash-chain check, with one honestly-disclosed residual (which contract you meant to ask about) pushed to the caller as a required, unforgettable parameter rather than hidden. Real group-arithmetic decision on 1-vs-2 atomic groups using M5's actual post-fix measured costs, not stale estimates. | 2026-08-01 | Approved by human. Sonnet implementation dispatched. |
| M7 | Receipt/log proof verifier | Not Started | — | M6 | **Owns the unsolved >4096B receipt-leaf problem** — 9/137 real receipts in the spike's own test block exceed the AVM value cap; no streaming hash opcode exists; implementation must not start until this specific design doc is explicitly approved, may force revision of M2 | 2026-07-30 | Blocked until M6; treat this design doc review as a hard stop |
| M8 | Trusted-root anchor contract | Not Started | — | M3, M6 | Root-history retention/eviction policy (real storage-cost tradeoff) | 2026-07-30 | Blocked until M3 + M6 |
| M9 | Off-chain relayer/client | Not Started | — | M5, M3 | Design can start once M4/M6/M7/M8 ABIs are frozen, ahead of their implementations landing | 2026-07-30 | Blocked until M4/M6/M7/M8 interfaces frozen |
| M10 | Deployment & box-storage schema tooling | Not Started | — | M7, M8, M9 | Mostly boilerplate once M8's retention policy is set | 2026-07-30 | Blocked |
| M11 | Real-data test harness & CI integration | Not Started | — | M9 | Live-docker-CI vs. pinned-fixture-CI split (both workflows exist from scaffold; this module ratifies the policy) | 2026-07-30 | Grows continuously from scaffold onward — revisit after every module lands |
| M12 | Docs & packaging / release prep | Not Started | — | M10, M11 | Contract-versioning story (AVM/consensus-fork-gated, not plain semver) | 2026-07-30 | Gates public v1 release |

## Locked decisions (do not relitigate without a new user conversation)
- Contracts authored in **Algorand Python (Puya)**, dropping to raw `Op`/inline TEAL only in budget-critical inner loops (RLP decode, MSM chunking).
- **Full Ethereum consensus-spec compliance from the start** — no stubbed BLS signature verification, no skipped SSZ track.
- Every module gate: Opus design doc → human approval → Sonnet implementation. No exceptions.
- No cost/budget claim ships without a real `simulate` response behind it (reuse `tests/fixtures/spike-reference/avm_bls_bench.py` / `mpt_bench.py` harness pattern).

## Full plan
See `/home/mark/.claude/plans/peppy-cuddling-snail.md` for the complete module rationale, dependency graph, and execution steps this roadmap was generated from.
