# Roadmap

This file is the single source of truth for "what's next." Read this first in
any new session before doing anything else.

**Workflow for every module**: an Opus design pass writes `docs/design/NNN-*.md`
and commits it → human reviews and approves the design doc → a Sonnet
implementation pass writes code + tests against it, and commits → this table
gets updated. Implementation never starts on an unapproved design doc.

**Legend**: Not Started / Design Drafted / Design Approved / Implementing / Implemented / Tested / Released

| # | Module | Status | Design doc | Depends on | Open questions inherited from spike | Last updated | Next session should |
|---|---|---|---|---|---|---|---|
| — | Scaffold | Implemented | — | — | — | 2026-07-30 | Push scaffold to GitHub, then start M1 design |
| M1 | BLS12-381 point codec & MSM/pairing wrapper | Implemented | [docs/design/001-bls-primitives.md](docs/design/001-bls-primitives.md) | scaffold | Resolved: trust boundary = subgroup-check + recompress-and-compare against SSZ-committed compressed bytes (bare subgroup check is provably insufficient, see doc §4.4 attack trace). **P1 now measured**: `ec_map_to BLS12_381g2` = 8185 (confirms the doc's 8150 citation). P3 spot-measured (g2 subgroup-check 2373, g2 add 338). P9 spot-check at small n confirms ec_add-chain glue is under the 45/iter threshold — chain stays correct default. | 2026-07-30 | 52/52 tests pass incl. live algod (contracts/primitives/bls/, tests/bls/). Gaps before "Tested": no `tests/bls/generate_fixtures.py` / persisted `tests/fixtures/bls/*.json` (design doc checklist item 8 requires this); P4-P8, P10-P11 unmeasured; P9 not measured at the real n=42 chunk size (hit the 2048B app-arg cap as design doc §7.3 predicts — needs the box-staging path). Not yet committed — awaiting human review. |
| M2 | On-chain optimized RLP decoder | Implemented, gates 4/6 (G2, G4, G5, G6) | [docs/design/002-rlp-decoder.md](docs/design/002-rlp-decoder.md) §16 | scaffold | **O-1/O-2 pulled forward per human direction, 2026-07-31 (§16)**: raw-TEAL injection investigated and found not cleanly achievable in the installed Puya/algorand-python toolchain without abandoning single-source-of-truth (a real, useful side-finding: `op.Scratch` with a compile-time-constant slot index DOES compile to cheap immediate-form `store`/`load`, so genuine manual scratch is possible within Puya — measured to bring no net win for this loop, so not used). O-2 (general table-free capture) was implemented and measured to bring **no net benefit** for a full 17-item walk (removed table-write cost is offset by added want-index compare cost — both ~equally dominated by Puya's fixed per-item control-flow cost, ~30-35 opcodes/item regardless). O-2 specialised to the always-exactly-2-item MPT ext/leaf case (`rlp_scan2`, no loop at all) and O-1 early-exit (`rlp_scan_upto`, walks only to the wanted index) were both shipped. **G6 (the gate that matters — real 8-node account-proof composition) now measures 2,566, beating the spike's 3,276 by ~22%** (was 5,302 pre-§16). G3 improved 291→192 (still misses the ≤90 target). G1 no longer has one clean number (cost is now index-dependent by design at the caller's choice between `rlp_scan_upto`/`rlp_scan2` and the unchanged flat-cost `rlp_scan`/`rlp_table_item`) — both are reported honestly in `bench/rlp_results.json` rather than forced to pass. G2/G4/G5 unaffected/still pass. `rlp_scan`/`rlp_table_item`/`mpt_node_scan` are completely unchanged and remain correct for repeated-access or full-arity-check callers. | 2026-07-31 | M5 design should use `rlp_scan2`/`rlp_scan_upto` (§16) as the default MPT-node descent entry points, falling back to `rlp_scan`/`rlp_table_item` only if a hop genuinely needs repeated access to the same node or a full arity check. |
| M3 | SSZ Merkle branch verifier | Implemented | [docs/design/003-ssz-verifier.md](docs/design/003-ssz-verifier.md) | scaffold | Resolved: sha256 measured at flat 35 budget (3.71x cheaper than keccak256); exact cost formula `53+61*depth+2*z` validated against 38/38 official consensus-spec test vectors across 6 forks (Altair-Gloas); fork decision = gindex is a runtime parameter, never hardcoded (spec itself is fork-parameterized at runtime). **NEW finding from implementation**: real Puya-compiled cost is ~40% higher than the design doc's hand-TEAL numbers (~`103+83*depth` vs `53+61*depth+2z`) — single-call depth ceiling is actually ~7-8, not 10. **M4 must use this revised ceiling, not the design doc's**, when sizing sync-committee/finality proof calls. | 2026-07-30 | 165/165 tests pass (contracts/primitives/ssz/, tests/ssz/). Gaps before "Tested": T4 (independent hash_tree_root check via remerkleable/pyspec) not done; full-scale 512-pubkey/99-call T13 not run live (only small-scale n=5 confirmed); ci-live.yml/ci-offline.yml still placeholders (M11's job). Not yet committed — awaiting human review. **2026-08-06, real bug found and fixed**: `merkleize_stack_finalize` silently returned the wrong root (`zero_hash(depth)` instead of the real one) whenever exactly `2**depth` chunks were pushed — a fully-packed Vector. The carry chain deposits a fully-packed root in stack slot `depth`, but the finalize loop only ever read slots `0..depth-1`. Invisible in this project's own test suite (`tests/ssz/test_merkleize.py`'s `NON_POWER_OF_TWO_NS` deliberately stops at 511) and invisible in every prior M3/M4 exercise, because none had pushed a fully-packed Vector before — a real `SyncCommittee.pubkeys: Vector[BLSPubkey, 512]` (`512 == 2**9` exactly, always, for every real mainnet committee) is the first shape that ever hit it, surfaced by M4's first live end-to-end install against real mainnet data (see M4's row and `tests/sync_committee/test_live_e2e_finality.py`). Fixed with a 3-line early return (`op.getbit(filled, depth)`); all 165 pre-existing `tests/ssz/` tests re-verified passing unchanged, independently, after the fix. |
| M4 | Sync-committee update verifier | Implemented, install-flow box-cap gap fixed and verified live | [docs/design/004-sync-committee.md](docs/design/004-sync-committee.md) §16 | M1, M3 | **Design-level findings confirmed correct in real code**: signing-root/DST construction (§3), `fork_version` at `signature_slot-1` trap, 28-byte domain truncation, and SSZ Bitvector[512] bit-order remap (new T5, 6/6 tests, proven via exhaustive round-trip) all verified against real vendored vectors. Also found and fixed a **test-harness bug** (not a contract bug) in the original committee-rollover tracking, requiring a hand-trace against real `consensus-specs@v1.6.0-beta.0` pseudocode — documented a minor labeling erratum in design doc §3.4's table (3 cells mislabeled current-vs-next committee; does not change any signing-root/BLS-result value). **§16, this pass: the install-flow box-cap gap is FIXED and verified live.** Measured (not assumed) against real dev-mode algod: the box-reference cap is exactly 8 per TRANSACTION (structural, confirmed via the literal protocol error), and there is a SEPARATE, real, per-box-reference budget of 2,048 B, POOLED ACROSS THE WHOLE ATOMIC GROUP (not the 1,024 B/ref the design doc originally cited, and not per-transaction) — both measured directly, several ways. `bootstrap`/`install_begin` are now validation-only (`INST_STATE_VALIDATED`, touching at most the `forks` box — a THIRD independent hit of the same cap was found and fixed this pass, since bootstrap's own `forks` read plus 8 key-box creates was also 9 refs in one call); two new ARC-4 methods, `install_open_keys` (`OPENING_BOXES`) then `install_open_session` (`INSTALLING`), split box creation across sibling transactions in the same atomic group, padded with `noop_budget()` calls to clear the pooled-budget floor (25 total refs measured minimum). Atomicity (not just the `inst_state` guard) is what prevents any partially-open box set from ever being observable on-chain. **Group count is UNCHANGED at 8** (§8.5) — the fix adds transactions within the existing first group's 16-slot budget, not new groups. **Live-tested end-to-end against real algod**: `tests/sync_committee/test_install_live.py`, 4/4 passing — the box-opening group actually commits (was blocked before), `install_chunk` proceeds correctly afterward, and the negative case (`install_chunk`/`install_open_session` called before box-opening completes) genuinely rejects. **Honest gap left open** (§16.5): a THIRD related but distinct finding — `box_extract`/`box_replace` on an existing box charges the pool the box's FULL declared size once per box per group, not the touched slice, so every `install_chunk` call needs >= 4 box refs (not 2) regardless of chunk size — is measured and documented but NOT re-derived across a full 512-member, 8-group install; only one small chunk was exercised live (via simulate, to isolate from the separate, pre-existing opcode-budget-donation mechanism). Does NOT affect §3/§5 correctness (signing root, bitfield, aggregation) — those remain solid. | 2026-07-31 | 284/284 tests pass project-wide (280 pre-existing + 4 new live install-flow tests), verified independently after every change in this pass. **Open follow-up for a future pass** (not blocking, not this task's scope): re-derive §8.5's box-reference bookkeeping for the full 512-member `install_chunk` sequence now that the full-box-size-per-touch charge is known, and confirm it live end-to-end (only a small chunk was tested here). Not yet committed — awaiting human review. **2026-08-06: real live end-to-end proof, real bug found in a shared dependency.** A brand-new `service/x402_endpoint/eth_beacon_rpc.py` client fetches real, currently-live sync-committee/finality data from real public beacon-chain light-client APIs (Nimbus's endpoints confirmed reliable). Against that live data, a full real 512-member committee install (64 real `install_chunk` calls + `install_finalize`) and a real `submit_update` both succeeded on real dev-mode algod, with on-chain `fin_slot`/`fin_root`/`fin_state_root` matching the live data exactly; a corrupted signature and a corrupted merkle branch were each genuinely rejected via real (non-simulated) submission. This was the first time `install_finalize` was ever exercised against a real, exactly-512-member `SyncCommittee.pubkeys` Vector, and it surfaced a real bug in M3's `merkleize_stack_finalize` (see M3's row) that made a fully-packed Vector's root come out wrong — fixed there, not in M4's own code. Fork-row parameters for the live "fulu" fork (`finality_gindex=169`, `current_sc_gindex=86`, `next_sc_gindex=87`) were derived from the beacon node's own spec/genesis endpoints and cross-checked three independent ways before being trusted. Test: `tests/sync_committee/test_live_e2e_finality.py`, 4/4 passing, run twice against fresh live data for reproducibility. Full project suite: 414/414 passing. **2026-08-07, this pass: `retire(gen)` (§8.4) wired to a real caller for the first time.** Real bug fixed, not in the contract (`retire()` itself, `contracts/sync_committee/verifier.py`, was untouched this pass): `grep -rn "retire" relayer/` returned nothing before this pass -- every real committee rollover permanently locked another generation's ~19.76 ALGO of key-box MBR with no automatic reclamation, even though the contract has always supported reclaiming it. `relayer/client.py`'s `EthAvmClient.sync(update=True)` now calls a new `retire_old_generations()` after its own update submission (default `auto_retire=True`, gated on `update` and on a configured signer): it scans every generation id ever allocated (`1..gen_counter`, on-chain state, no local bookkeeping), skips `cur_gen`/`next_gen`/`inst_gen` and any generation whose `a:<gen>` total box is already gone (a free `application_box_by_name` read, avoiding an ever-growing stream of wasted no-op transactions as `gen_counter` climbs over the project's lifetime), and submits a real `[retire, noop_budget...]` group for the rest -- per-generation failures are caught individually (`failed` list) so one bad generation never aborts the rest of `sync()`. `retire(gen)` deletes ALL 8 key boxes (6,144 B each) plus `a:<gen>` (96 B) unconditionally -- measured live this pass to need the exact same `plan_box_refs` closed form already proven for `submit_update`'s k=8 worst case (49,248 declared B -> 25 refs -> 4 transactions, `relayer/group/boxes.py`'s new `m4_retire_box_sizes`), the first real confirmation that a DELETE is charged the pooled per-ref write budget at a box's full declared size exactly like a read/write is. **A second, independent real bug found and fixed while building the required live rollover test** (`relayer/client.py`'s `_drive_m4_update`): the box-reference plan for a `submit_update` call always assumed `cur_gen`'s key boxes even when the signature period is `store_period + 1`, in which case the real contract reads `next_gen`'s key boxes instead (`verifier.py` step 5) -- meaning `sync(update=True)` could never have driven a real rollover at all before this fix, independent of `retire()` never being called; fixed by computing `using_next`/`committee_gen` client-side with the contract's own rule before planning box refs. **Live proof** (`tests/relayer/test_retire_live.py`, 3/3 passing): since a real rollover needs a real signature from period P+1 and the live-now period cannot supply that without a ~27h wait, this pass drove a real rollover against real, already-elapsed, BLS-signature-verified mainnet periods P=1810/P+1=1811 (both well after fulu's own activation period 1607) instead -- genuinely real consensus data throughout, only the FETCH TIME is not "just now". Measured, not assumed: `account_info` min-balance dropped **39,856,700 -> 20,095,800 microALGO (delta 19,760,900 microALGO = 19.7609 ALGO)**, cross-checked against `deploy/mbr.py`'s `box_mbr` formula applied to the real declared box sizes rather than hardcoded, and matching design doc 010 §10.2's own independently-measured figure exactly; `amount` stayed unchanged at 70,000,000 microALGO across the whole call. **Honest correction to this task's own framing**: box MBR is a requirement on already-resident funds, not an escrow -- `retire()` issues no inner `Payment` (confirmed by reading `install_delete_committee_boxes`), so `amount` does NOT increase; what "the ALGO comes back" really means, confirmed by a standalone box-create/delete probe against a throwaway app before writing the real test, is that `min-balance` drops by exactly the refund, freeing that same real, already-resident ALGO as spendable headroom (`amount - min-balance` rose by the identical 19,760,900). **A second real `retire(1)` call on the same already-retired generation was also measured, not assumed, to COMMIT as a harmless no-op** (AVM `box_del` on an already-missing box name returns false, no assertion -- `retire`'s own asserts only reject `gen == cur_gen/next_gen/inst_gen`, there is no separate "already retired" flag on-chain) -- `min-balance` was confirmed unchanged after the second call, i.e. no double refund, even though the call does not literally fail the way this task's own framing anticipated. A brand-new, zero-install contract and a single-generation genesis install were both confirmed to make `retire_old_generations()` a true no-op (no transactions submitted). Full project regression after this pass: 549 passed, 1 skipped, 4 deselected (pre-existing `test_live_e2e_finality.py`), 1 error -- `tests/state_anchor/test_live_e2e.py::TestG1M8RealDirectAnchor` hit the SAME pre-existing, live-data-dependent `box read budget (N) exceeded` fragility this project's own standing convention already names (M9/M10 rows); re-run in isolation immediately after and reproduced the identical error signature (`N=20480` this time, vs. `N=18432` previously -- real current mainnet participation shifting between runs, exactly as documented, not a regression from this pass, which never touches `tests/state_anchor/`, `contracts/**`, or `deploy/`). Not yet committed -- awaiting human review. **2026-08-07, this pass: MULTI-CYCLE proof -- the single-cycle result above cannot, by itself, rule out drift/leak/compounding across repeated real rotations.** Extended `tests/relayer/test_retire_live.py` with a new `test_r4_three_consecutive_real_cycles_no_drift_no_leak` (plus its own independent `historical_period_data_multi`/`env_retire_multi` fixtures and `_discovery_resubmit`/`_install_ahead` helpers, factored out of `env_retire`'s own inline body so they run three times without hand-copying), driving THREE full real install-ahead -> rollover -> auto-retire cycles back to back against four real, already-elapsed mainnet periods (1810->1811->1812->1813), through the SAME production `EthAvmClient.sync(update=True)` entry point `test_r1` already proved for one cycle -- not a re-derivation of `retire()` in isolation. Real, measured result against real dev-mode algod (app 443805), identical every cycle: min-balance rose 20,095,800 -> 39,856,700 microALGO (install-ahead peak, +19,760,900, `deploy/mbr.py`'s formula applied to real declared box sizes, never hardcoded) while the old generation was still live, then dropped back to EXACTLY 20,095,800 after each auto-retire -- cycle 1 (retire gen 1, period 1810->1811, promotes cur_gen=2), cycle 2 (retire gen 2, period 1811->1812, promotes cur_gen=3), cycle 3 (retire gen 3, period 1812->1813, promotes cur_gen=4); `amount` stayed unchanged at 70,000,000 microALGO through all three. Peak and post-retire baseline were the exact same number in every cycle (box names are fixed-width per `relayer/group/boxes.py`, so MBR is content-independent) -- no drift, no leak, no compounding. A real `application_boxes` listing after all three cycles confirmed zero orphaned boxes for any retired generation (all 8 key boxes + the total box gone for gens 1-3; gen 4, the only still-active generation, fully present) -- `retire()`'s cleanup is complete every cycle, not partial. **No new bug found in the retire()/sync() wiring itself this pass** (unlike the single-cycle pass, which found two real bugs) -- this test proves the existing fix holds up under repetition, it does not add a further fix. `tests/relayer/test_retire_live.py` overall: 4/4 passing (`test_r1`/`test_r2`/`test_r3` unchanged, plus this pass's `test_r4`). Full project regression: 547 passed, 1 skipped, 4 deselected (`test_live_e2e_finality.py`), 4 errors -- all four confined to `tests/relayer/test_live_relayer.py`'s `env_a`-dependent tests (`test_l1`/`test_l3`/`test_l4`/`test_l8`), none in this pass's own changes. Confirmed, via an independent isolated re-run of that file alone (which reproduced 3 of the 4 errors, with the specific failing subset differing nondeterministically between runs but the root-cause exception identical both times), that this is the SAME real, live-"now"-mainnet-data-dependent fragility already named for `test_l4`: today's real, current sync-committee participation drives `relayer/client.py`'s pre-existing `_submit_update_group`/`submit_run` donor-sizing (code this pass never touches) to a real Algorand `too many inner transactions` structural cap at roughly 300 donors -- the same CATEGORY of live-real-data fragility this project's own standing convention already names for `tests/state_anchor/test_live_e2e.py`'s box-read-budget flakiness (M9/M10 rows), just a different budget/code path, and out of this pass's scope (it lives entirely in `env_a`'s "live now" fixture, not in anything `test_r4`'s historical-period design touches or could have surfaced). This pass's own `test_retire_live.py` (historical-period-only by design, precisely to avoid this class of live-data nondeterminism) passed 4/4 clean in both the full run and the isolated re-run. Not yet committed -- awaiting human review. |
| M5 | MPT path-walker / node verifier | Implemented, real-submission fix landed (§16), budget gates still open | [docs/design/005-mpt-walker.md](docs/design/005-mpt-walker.md) §16 | M2 | **Security fix resolved**: branch/extension/leaf descent all derive their index/comparison from the key itself on-chain (`nibble_at(key, depth)`), never from a caller argument — verified against all three real fixture proofs, with the account-proof indices matching M2's own G6 bench exactly. Leaf match requires `depth + n_path == key_nibs` **exactly** (not `<=`), which is what rejects a strict-prefix leaf. **Args-vs-box resolved**: raw app-args, segmented walk, one atomic group, no boxes; segment hand-off verified via `gtxn LastLog`, tested live for both honest-pass and forged-reject. **§16, this pass**: found and fixed two real, measured implementation inefficiencies flagged after the first implementation pass shipped ~2x over target — `mpt_descend` was redecoding the same node twice per branch hop (once in `mpt_walk_node` to learn arity, again inside `mpt_branch_hop`), and `W`'s 101-byte buffer was fully reconstructed by concatenation every hop instead of splicing only the ≤35 bytes that change. Fixing both recovered a REAL, measured 1,303 budget (20%) on the headline 8-node account walk (6,419 → **5,116**) and 383 (17%) on the 3-node receipt walk (2,196 → **1,813**), with zero change to §5's security logic (72/72 mpt unit tests + 350/350 project tests pass unchanged). **Inner-transaction budget donors wired up** (§7.6's named escape hatch, M1 §9.1 / M4 §16's pattern): `MptSegmentApp` now issues no-op inner app calls to a separate donor callee before doing real work. **Headline result: a REAL, non-simulated submission of the full 3-segment, 8-node account-inclusion-proof group now succeeds end-to-end** (6 donor calls), and the 2-segment receipt-proof group also succeeds (2 donor calls) — this was flatly impossible before this pass (a single segment failed outright with `dynamic cost budget exceeded`, not merely under-`simulate`d). **Honest gap left open**: G6-M5 (5,116 vs <3,276 target), G1-M5 (1,813 vs <1,121), and G5-M5 (1,969 B vs ≤1,400 B compiled size) still do not pass; §16.4 attributes the remaining ~2,550-budget gap to real Puya-compiled control-flow/subroutine cost across §5's genuinely larger (correctly key-bound) surface versus M2's G6 baseline, not to further double-work — no additional double-decode or redundant-copy waste was found. The design's own named lever (O-M5-1, shape-based discriminator) would close only ~4% of the remaining gap. | 2026-07-31 | Budget/size gates (G1/G5/G6-M5) remain open for a future optimization pass; §16.4 discusses the tradeoffs (revisiting §5.1's unconditional-discriminate architecture, or accepting the security-fixed walker's real per-hop cost). M6 may proceed against the security-verified, real-submission-capable walker as-is. **2026-08-07, same security fix applied here too (found via M10 S9.1)**: `MptSegmentApp` had the identical missing-`OnCompletion`-check gap as M7's `Mpt7ReceiptApp` (same raw-`Contract` shape). Not deployed to mainnet, so no live exposure, but fixed for consistency before any future deployment. Re-verified live: real non-creator update/delete attack against the patched contract now correctly rejected. |
| M6 | Account & storage proof composer | Implemented | [docs/design/006-account-storage-proof.md](docs/design/006-account-storage-proof.md) | M5 | **Resolved**: exclusion at composite level answered for both cases (account absent -> trivial slot-absence, no phase B needed; account present but slot absent -> a real exclusion proof against the verified storageRoot). **Security-critical hand-off, verified in real code**: `mpt6_state_from_prev(gi)` takes ONLY a group index -- no root/key/slot parameter exists to trust or forge. The §5.4 attack scenario (relayer substitutes a different root's proof for phase B) is genuinely rejected via M5's own `W11` hash-chain check, tested against real mainnet data. TP-M6-3's residual (which contract you meant to ask about) is enforced by `mpt6_result_from_group` requiring `want_*` args the consumer cannot omit. **First module to land UNDER its design-time budget prediction** (12,202 measured vs ~12,500 predicted for the full 5-segment composite) rather than over it. **Real non-simulated submission of the full composite succeeded** (14 donor calls, one 16-txn group) -- G2-M6/G3-M6 both pass. | 2026-08-01 | 403/403 tests pass project-wide (independently verified). Honest gaps: G4-M6 (segment-boundary cost isolation) not separated out, same as M5; absent-slot test uses derived (not real eth_getProof) fixtures; `Mpt6ComposerBare` (single-tx bare baseline) exceeds the 8,192B deploy cap so wasn't measured live, a phase-B-only bare contract was used instead. Not yet committed -- awaiting confirmation to commit and push. **2026-08-07, same security fix applied here too (found via M10 S9.1)**: `Mpt6ComposerApp` had the identical missing-`OnCompletion`-check gap. Not deployed to mainnet, so no live exposure, but fixed for consistency before any future deployment. Re-verified live: real non-creator update/delete attack against the patched contract now correctly rejected. |
| M7 | Receipt/log proof verifier | **Implemented (T1), live-tested 2026-08-05.** `contracts/receipt/{state,decode,handoff,box,bench_app}.py` — mirrors M6's structure exactly. Compiles clean with `puyapy` (3,104 B). Real headline test (tx 31, block 25,639,768, the design doc's own pinned example) verified live against an isolated dev-mode algod: `R_INCLUDED`, real `receiptsRoot` match, `tx_type=2`, `n_topics=4`, `n_logs=2`, `data_hash` = real `keccak256("")` — via a REAL non-simulated submission (round 7), not just simulate. Negative case also proven live: `log_index >= n_logs` correctly returns `R_NO_SUCH_LOG` as a clean result, never a failed transaction (§5.4's core safety property). Real budget measured at 3,790 for the full walk+decode (vs. M5's own 1,813 for the walk alone) — exceeds a single top-level call's ~700 base, needs atomic-group budget pooling (filler NoOp calls to the same app; **plain Payment transactions do NOT pool opcode budget, only application calls do** — a real, previously-undocumented-here finding). **`MODE_NEXT` and T2 both closed out same day**: `MODE_NEXT` proven by splitting tx 31's real 3-node proof across a 2-txn group (round 10). T2 proven against a real T2-range receipt (tx 7, leaf 2,453 B, reconstructed for real from `block_receipts` since only tx 31 had a pre-built fixture) — full box-staging sequence (open/write x2 chunked/extract+walk+decode/close) across a 9-txn atomic group, real submission confirmed round 15, on-chain log verified byte-identical to the simulated one. Real finding along the way: box MBR is charged to the **app account**, not the sender — needs funding before `box_create` in the same group. M7's T1/T2 scope is now fully implemented and live-verified, real submissions only, no gaps left. **Deployed to real Algorand mainnet same day: app id `3664247481`** (round 63789985), re-verified against two more completely fresh real Ethereum receipts. **Then the real x402 challenge requirement itself, proven end-to-end**: `service/x402_endpoint/` (real Ethereum RPC + trie reconstruction generalized to any block/tx, real M7 relayer, FastAPI + GoPlausible's actual `x402-avm` package) tested with two freshly-generated funded mainnet wallets — client paid, GoPlausible's real facilitator verified/settled, service verified the receipt on-chain, **0.01 USDC actually moved** (confirmed via before/after balance checks, not just a 200 response). Two real bugs found and fixed: the x402 client signer needs raw msgpack bytes, not algosdk's base64-string convention; and **both sides of a payment need the asset opted in** — missed the receiving wallet's USDC opt-in first, got a real "must optin" error back from the facilitator's own simulation. Not yet done: public HTTPS exposure, Bazaar discovery registration, and the actual `x402-global-challenge` submission tag — separate, more exposed steps. **Correction (M12, 2026-08-08, measured)**: public HTTPS exposure is done — `GET https://x402endpoint-nu.vercel.app/health` returns real data (`{"algod_round":63855556,"m7_app_id":3665914633}`); this row was never updated after the 2026-08-07 Vercel deployment recorded below. Bazaar registration remains declined (docs/design/012-docs-packaging-release.md §8: nothing monitors the live service) and the challenge submission tag remains sequenced, not pressed. T3 (tiers A/B/C) remains fully designed, real-proven, and documented but explicitly deferred/unimplemented. | [docs/design/007-receipt-log-proof.md](docs/design/007-receipt-log-proof.md) §3 (T1/T2, implemented), §4.5, §4.12–§4.14, §13, §14.6–§14.8 (T3, reserved) | M6 | **T1/T2 unchanged and solid** (93.4% real coverage, direct + box-staged native keccak). **T3's ZK mechanism is now demonstrated end-to-end on real data, not just designed**: revision 2's own recommended spike was run this pass and **ZK-B1 and ZK-B2 are both closed**, along with ZK-B3/B5/B6 and half of ZK-B7. **Headline: a real PLONK proof of a real receipt-trie leaf from the fixture block, generated against the REAL Perpetual Powers of Tau ceremony (not a TestOnly setup), verified by an AlgoPlonk logicsig in a REAL non-simulated submission** (rounds 557/559/560) — twice, once for a typed EIP-1559 receipt and once for a legacy one. Measured: **185,370 logicsig budget, 2 app budget, 864 B proof, 3,924 B verifier** (BN254 — revision 2's BLS12-381 figures were 221,201 / 40 / 1,184 B / 4,819 B). **ZK-B2's resolution corrects the design's own plan**: AlgoPlonk's `setup` registry is `go:embed`-based and closed to consumers, so you cannot "add a setup" — but you do not need to; bypass `setup.Run`, build the SRS from a real `.ptau` with `mdehoog/gnark-ptau` (the converter AlgoPlonk's own audit script uses — **no format incompatibility**), and hand the keys to AlgoPlonk's unmodified codegen. Also **reproduced AlgoPlonk's vendored PPOT `pk.bin` byte-for-byte** from the published ceremony file. **Real circuit: 28 compiles, measured formula, 0.065% residual**; revision 2's tier-A point really does fit 2²⁴ at **16,293,891** constraints. **Coverage corrected DOWNWARDS on real numbers**: a tier is a pair `(N, LOGMAX)`, because the largest single log costs the same 224,269/keccak-block the leaf does — 2²⁴ gives **97.8%** (not the projected 98.5%), 99.3% needs **2²⁶** (not 2²⁵), tx 119 needs **2²⁹** (not ~2²⁸). Circuit soundness: **97/97 real receipts satisfied, 0 failed**, and every negative test rejects including **span containment** (§4.4's "single most important constraint"). **§13 is a new Part-4 assessment for the human**: real in-circuit cost data for BLS12-381/sha256/keccak256 vs M1–M6's real on-chain budgets — **recommendation is NO pivot**, keep M1–M6 as they are. | 2026-08-04 | **Revision 4 (§14) found 2 real, CONFIRMED security bugs** via dedicated review — this is exactly why the review was worth doing. (1) **AlgoPlonk itself**: silently-discarded errors leave the off-chain KZG pairing check vacuous — demonstrated with a real corrupted proof that verified when it shouldn't have. On-chain verification unaffected; off-chain gate was not real. Two-line fix identified (in `ptaufast`), not yet upstreamed to AlgoPlonk. (2) **This project's own draft circuit**: `LogIndex` public input is under-constrained — `LogIndex = P-1` degenerates the log commitment to `keccak256("")` and a REAL forged proof verifies. Not exploitable end-to-end (two on-chain checks catch it in practice) but a genuine gap in the circuit's own self-containment claim. Fix identified, not applied (kept for reproducible constraint counts). Also found: the memory bottleneck was mis-identified in revision 3 — the expensive step was redundant, not slow-but-necessary; a `ptaufast` loader reads precomputed values already in the ceremony file instead of recomputing them (byte-identical, verified) for a real 66% memory / 330x speed improvement on SRS acquisition. Net effect: once-per-tier **setup** now plausibly fits 32GB (was 64GB+); **proving** is unchanged and still wants ~64GB at the full 2²⁴ tier — so the local 14GB machine still cannot run the full pipeline, but a cheaper cloud rental now covers setup. O-M7-4 circuit-efficiency attempt: modest real gain (97.8%→98.5% coverage on tested case) at the cost of added protocol complexity (tier becomes a 4-tuple). Dependency pinning confirmed clean, no advisories against pinned versions. **Revision 6 (§14.6, 2026-08-05): ZK-B4 CLOSED for real.** Ran the full pipeline on an AWS `r7i.2xlarge` (64GB RAM) — the class of host revision 4 said would be needed. Real tx-1 receipt, real tier-A circuit (16,294,913 constraints), real PPOT `_24` ceremony (18GB, sha256 pinned in `generated/tierA/M7VerifierTierA.report.json`): compile 14.1s, SRS load 23.8s, Lagrange SRS build 1,773.4s (~29.6min, the dominant cost, confirms revision 4's ~40min projection), `plonk.Setup` 45.0s, **`plonk.Prove`+`plonk.Verify` 203.7s — the step that had never run above 2²¹ before this pass**. Peak RSS **29.99 GB** (higher than revision 4's ~16GB extrapolation, still comfortably inside 64GB — the recommendation was right). Then went further than a bare proof: **real on-chain verification**, `puyapy` compiled the AlgoPlonk-generated logicsig (3,926B), deployed to a real dev-mode algod (Docker, same host), **186,954 logicsig budget / 2 app budget consumed, negative test (tampered public input) correctly rejected, and a REAL non-simulated submission confirmed on-chain (round 2, app id 1002)**. Two toolchain gotchas recorded in §14.6 for reproducibility: `pip install puya` gets the wrong (incompatible) package — the correct one is the separately-versioned `puyapy` PyPI package at 5.9.0; and `puyapy` needs the venv's `bin/` first on `PATH` or it shells out to the system Python and fails. Generated `.py`/`.teal`/`.proof` blobs deliberately not committed per this directory's own README; only the report JSON is kept. ZK-B9's tier-selection design question is unaffected by this pass. **Revision 7 (§14.7, same day): ZK-B8 CLOSED too.** Proved tier B (2²⁵, real tx-73, 19,688,551 constraints) and tier C (2²⁶, real tx-6, 43,353,550 constraints) end-to-end and on-chain, same method as tier A. Tier B: 1h9m25s, 44.6GB peak, round 4/app 1019. Tier C: needed a bigger host (`r7i.8xlarge` was requested but blocked by a 16-vCPU account quota; used `r7i.4xlarge` instead) — 1h13m48s (faster than the 2.5-3hr estimate thanks to 2x the vCPUs parallelizing well), but peak RSS hit 95.2GB, higher than extrapolated from tiers A/B, confirming memory scaling shouldn't be extrapolated across vCPU-count changes. Round 6/app 1036. All three logicsig budgets (186,954/186,838/187,730) land within 0.5% of each other across a 4x domain range — strong confirmation that on-chain verification cost doesn't grow with statement size. **99.3% (8/9 oversized receipts) is now this design's real, on-chain-verified coverage ceiling**, not a projection; tx 119 is confirmed permanently unreachable (compile alone OOMs at 64GB; needs ~2²⁹, beyond any real ceremony). Two operational gotchas recorded in §14.7 for reproducibility: `--timeout-seconds` on `aws ssm send-command` does not cap script runtime (use the `executionTimeout` document parameter instead, or SSM silently SIGKILLs a long run at the 1-hour default); and Docker containers need an explicit restart policy or they don't survive an EC2 stop/start cycle. **Revision 8 (§14.8, same day): two corrections to numbers the rest of this doc had been treating as final.** (1) Pulled 300 real blocks / 94,667 real receipts via public RPC (`eth_getBlockReceipts`) — the 2-block fixture this whole project measured coverage against overstated how often ZK is needed by ~3x: real T1+T2 coverage is 97.5% (not 93.4%), only 2.2% of real receipts need any ZK tier (not 6.6%). (2) Built `cmd/setupkeys`/`cmd/provewith` to separate PLONK's one-time setup (compile+SRS+Lagrange+Setup, ~32min, produces a reusable proving key) from per-receipt proving — every prior cost figure in this doc bundled both together. Real test: proved two different real receipts (tx 1, tx 2) from one cached tier-A setup, both verified correct, **$0.036/proof and ~4.1min marginal cost — 8.3x cheaper than the $0.30 full-pipeline figure**. Tier B/C marginal costs projected (not yet measured) at ~$0.065/~$0.16. Net effect on x402 toll viability: previously estimated needing a ~2.7x toll increase to pool tiers A+B+C; with real trigger rates and real marginal cost, the existing 0.01 USDC toll pools all three tiers with ~5.8x margin to spare. **2026-08-07, real security fix (found during M10's design pass, S9.1): `Mpt7ReceiptApp` accepted `UpdateApplication`/`DeleteApplication` from ANY account with a zero-app-arg call** -- confirmed by real live experiment against a fresh dev-mode instance (non-creator update and delete both succeeded before the fix). Root cause: this class is a raw `Contract`, not `ARC4Contract`, so nothing in its hand-rolled dispatch ever checked `Txn.OnCompletion` (M4/M8 are immune by construction -- Puya's ARC4 router rejects every on-completion but `NoOp`). **Fixed**: `assert Txn.on_completion == OnCompleteAction.NoOp` as the first statement in `approval_program`, matching M4/M8's own `txn OnCompletion; !; assert` prologue exactly. Re-verified live, directly: the identical non-creator update/delete attack against the PATCHED contract now fails with a real `assert failed` at the new check, for both attack shapes. `tests/unit/` (175 tests) and the combined-chain live test re-run clean, unchanged. **This mainnet app id (`3664247481`) is affected and is the reason this was fixed immediately rather than deferred** -- redeploying it is a separate, pending step (new app id, `service/x402_endpoint`'s `M7_APP_ID` config must be updated, old app left as-is with 0 ALGO at risk in its own account per real balance check). Not yet committed at time of fix -- see this session's own commit for the real diff. **2026-08-07, real mainnet redeploy of the fixed contract**: the patched `Mpt7ReceiptApp` (with the `OnCompletion` check) was deployed for real to Algorand mainnet by the same deployer account (`service_wallet`) that created the original app -- **new app id `3665914633`**, funded with 200,000 microALGO. Verified two ways before trusting it: the deployed program's SHA-256 matches the locally-verified fixed contract byte-for-byte, and the deployer's real `created-apps` list on mainnet shows both the old and new app ids. **The old app id (`3664247481`) is no longer the one to use** -- no real `.env` file exists anywhere in this repo's checkout (only the blank `.env.example` template), so there was no live process to restart; whoever runs `service/x402_endpoint` for real must set `M7_APP_ID=3665914633`. Decision on the old, vulnerable app (currently 0 ALGO, still live and still hijackable by anyone) -- leave it inert or delete it -- is a separate, pending call. **Correction (M12, 2026-08-08, measured)**: this app no longer exists -- `GET https://mainnet-api.algonode.cloud/v2/applications/3664247481` -> `{"message": "application does not exist"}`, independently re-confirmed this pass. The pending disposal decision above is resolved by this fact (deleted, by a party this repo does not know), not by anyone acting on the "leave it inert or delete it" choice (docs/design/012-docs-packaging-release.md §2.5c). **2026-08-07, real Vercel deployment, live and paid-tested end to end**: `service/x402_endpoint` is deployed for real at `https://x402endpoint-nu.vercel.app` (Hobby/free tier). Three real deploy-time findings, none of them guessable from local dev-mode testing: (1) Vercel only uploads whatever directory `vercel deploy` is run from -- `relayer/` had to be included by deploying from the repo root (with a new root-level `vercel.json`/`.vercelignore` excluding `contracts/`/`tests/`/`docs/`/`bench/` to keep the upload small) rather than from `service/x402_endpoint/` alone. (2) Vercel's Python/FastAPI builder reads dependencies from the repo root's `pyproject.toml` exclusively once `[tool.vercel]` names an entrypoint there -- `requirements.txt` is not consulted at all for the deployed build, confirmed by a real crash (`ModuleNotFoundError: fastapi`) before `fastapi`/`uvicorn`/`x402-avm` were added to `pyproject.toml`'s own dependency list too (kept in sync by hand with `requirements.txt`, nothing enforces this yet). `service/__init__.py` and `service/x402_endpoint/__init__.py` were added so `service.x402_endpoint.main:app` resolves as a dotted entrypoint. (3) A real, non-simulated `DonorIssuer`/`DonorCallee` pair had to be deployed to mainnet (`3666047587`/`3666047636`, same deployer account) and configured (`DONOR_CALLEE_ID`/`DONOR_ISSUER_ID` env vars) before `EthAvmClient.prove_receipt()` would run at all -- it hard-requires this pair per §7.1's design (M9 §18 item 5), and none existed for this fresh mainnet deployment. **A real, complete, non-simulated end-to-end paid request then succeeded**: a real 0.01 USDC payment from a funded client wallet, verified by GoPlausible's real facilitator, triggered a real fetch of Ethereum block 25,691,209's real receipts, a real trie proof, and a real submission to the fixed `Mpt7ReceiptApp` (app `3665914633`) via the new donor pair -- returning `rstatus_name: "R_INCLUDED"` with the real receipt fields, confirmed on-chain at round 63,837,827, with the payment itself landing in `PAY_TO_ADDRESS` exactly as expected (0.01 -> 0.02 USDC, verified by a real balance query, not assumed from the HTTP response alone). **Vercel Hobby's real function-duration limit is 300 seconds (Fluid Compute, confirmed against Vercel's own current docs), not the 10-second figure this session initially assumed** -- the configured `maxDuration: 60` was never at risk, and this whole class of concern was a non-issue once checked against the real, current platform behavior rather than stale assumptions. |
| M8 | Trusted-root anchor contract | Implemented, live-tested (G1-M8, G5-M8) | [docs/design/008-trusted-root-anchor.md](docs/design/008-trusted-root-anchor.md) | M3, M4 | Root-history retention/eviction policy (real storage-cost tradeoff) — **resolved, §7**, see below | 2026-08-06 | `contracts/state_anchor/*.py` (constants/forks/record/bridge/box/handoff/anchor_app/bench_app), `tests/state_anchor/*.py` (conftest/synth/real_ssz/test_core/test_live_e2e), 16/16 tests pass (14 structural+security+retention via real dev-mode `simulate`/`submit`, 2 real non-simulated live). Project-wide regression check: 411/411 pre-existing M1–M7 tests still pass unchanged (3 pre-existing M4 live tests deselected — see below, not caused by this pass). **G1-M8 (the headline gate) passed live, but against CURRENT data, not the design doc's pinned example**: mainnet block 25,639,768 is now ~11 days old, far outside HISTORICAL mode's 8,192-slot (~27.3h) window, so it is no longer anchorable by any v1 mode — substituted the CURRENT live finalized block (EL block 25,694,842 at the time this ran) for the same property under test. Real chain: a FRESH real 512-member sync committee was installed and a real `submit_update` finalized live data (same procedure as M4's own live test), then a real, independently-computed SSZ `ExecutionPayload` Merkle tree (`tests/state_anchor/real_ssz.py`, hand-rolled from the 17 real Deneb+ fields fetched from the beacon API's own `finality_update` response, since M8's deep-fold branches are NOT what the beacon API's own `execution_branch` provides — that proves only the whole payload header at depth 4) produced real depth-9 branches for `state_root`/`receipts_root`/`block_number`, independently cross-checked against real live data three ways before being trusted: composed gindices 802/803/806 matched the design doc's own citations; folding `block_hash`'s branch independently reproduced the spec-published `EXECUTION_BLOCK_HASH_GINDEX_DENEB=812` exactly; and all three folds reproduced the REAL `body_root` the beacon API published in the same response. A real, non-simulated `anchor_direct` (12 real donor inner calls) committed, and a real `attest` returned `el_receipts_root` byte-identical to the real chain's `receiptsRoot` for that exact live block — this is the actual "trustless" claim TP-M7-2/G1-M8 exist to prove, demonstrated on live current data end-to-end. **G5-M8 also passed live**: a real, non-simulated 16-transaction `ring_init_chunk` group filled all 128 boxes at `N=128` in one atomic group, `frozen` cleared to 0 on completion, matching §7.7/§7.8 exactly. **Real measured budgets** (vs §10's projections): `anchor_direct` **3,588–3,652** (projected 4,686, **~22–24% UNDER** — this project's third data point on projection accuracy, joining M3 (40% over) and M5 (2× over a gate) on the "over" side and M6 (under) on this side — direction is genuinely unpredictable per-module, confirming §16 item 1's own prediction); `attest` alone **64** (projected 260, **75% under** — `attest`'s hot-path really is as cheap as §10.4's headline claim argued, if anything more so). Compiled sizes: `TrustedRootAnchor` **3,027 B** (projected ~2,200, 38% over, same direction as M3/M5's own hand-estimate misses) — comfortably inside the 8,192 B cap (63% headroom) but needs `extra_pages=1` to deploy (exceeds one 2,048 B page); `AnchorReceiptProbe` (the M7-combining live-proof probe) **3,208 B**. **A real Puya (5.9.0) compiler quirk was found and worked around, not silently patched around**: assigning a global-state `UInt64` field directly from a chained call expression (`self.x = some_arc4_value.native`) intermittently produces `error: unrecognised member` at EVERY later use site of that field, for reasons this pass could not fully isolate (it is not simply "any non-literal RHS breaks it" — `self.gov = governance.native` on an adjacent line compiles fine every time). The reliable, reproduced fix — binding the value to an explicitly `UInt64`-annotated local first, then assigning the local — is documented in `anchor_app.py`'s `create()` docstring; every other module in this codebase happened never to hit this pattern. **A second real, measured finding that corrects the design doc's own pseudocode**: §8.3 assumes `attest`'s ARC-4 return log is `0x151f7c75 ‖ <2-byte length> ‖ 154 bytes` (160 B total, M5/M6/M7's own hand-built envelope convention) — but `TrustedRootAnchor` is a genuine `ARC4Contract`, and Puya's own auto-generated return-log for a fixed-size `arc4.StaticArray[Byte, 154]` return value carries **no length field**: **158 bytes total**, confirmed against a real dev-mode algod probe before `handoff.py`'s `anchor_from_group` was written against it. **A third finding**: an `algopy.Application`-typed ARC-4 method parameter (used for `m4_app`) compiles to ARC-56 type `uint64`, not the ARC-4 "application" reference type — the real app id must be passed as a plain integer AND separately listed in `foreign_apps`/`Access`, not resolved automatically by an ABI-aware client the way a true reference type would be; recorded here since a reviewer reaching for `Application` expecting reference-type ergonomics will be surprised. **G8-M8 done**: `contracts/state_anchor/TrustedRootAnchor.arc56.json` committed as the frozen ABI artifact (not yet CI-diffed — that is M11's job). **Retention policy (the row's original open question) resolved exactly as designed**: fixed prepaid hot ring (`N`, tested at `N=8` structurally and `N=128` live) with implicit modular eviction (`ring_admit_and_write`, tested: N-ADMIT rejection, oldest-evicted-newest-kept, N12/N13 absent-vs-revoked distinction) plus an opt-in pinned tier (`pin`/`unpin`, tested: MBR-funded creation, payer-only refund). **TP-M8-4 (the most explicitly flagged risk) resolved and tested two ways**: `ANCHOR_APP_ID` is a `handoff.py` module-level compile-time constant (never a parameter/global/box), confirmed by (a) a real forged-app-id adversarial test (`FakeAnchor`, identical ARC-4 selector and envelope, attacker-chosen payload) genuinely rejected when a real consumer compiled against the real `TrustedRootAnchor` id is pointed at it instead, and (b) direct inspection of the compiled TEAL confirming the id is a `pushint` immediate. **§9.2's `mpt7_result_against_anchor` is real and compiled** (`handoff.py`), built by importing M7's own unmodified `mpt7_result_from_group`/receipt-walk subroutines into a NEW bench-only class (`AnchorReceiptProbe`) rather than editing `contracts/receipt/*.py` — per this task's explicit scope boundary, M6/M7's own deployed contracts were not modified this pass, so this mechanism is proven correct (adversarial test above) but not yet wired into `Mpt7ReceiptApp`/`main.py`/`m7_relayer.py`, which remains a separate, later integration task exactly as scoped. **§6.2's `ring_n` immutability is enforced and tested**: no setter exists anywhere in the ABI surface (asserted directly against the compiled ARC-56 method list), `ring_n` must be a nonzero power of two at `create` (tested, rejects 7), and every box-key/eviction computation derives from the single immutable global. **§9.3's T2 cache-miss group-size fix is demonstrated as a general mechanism, not wired into M7**: this pass's own `anchor_direct`/`anchor_historical` real submissions use exactly the donor-inner-call pattern (`DonorIssuer`/`DonorCallee`, reused verbatim from M4's `bench_app.py`) the design doc recommends replacing M7's 8 filler NoOps with — proven live, real budget pooling confirmed — but rebuilding M7's own T2 group with it was correctly out of this pass's scope (M6/M7 contract files untouched) and remains M9's to do. **Honest gaps, stated plainly**: (1) **§3.4's Deneb→Electra two-row fork trap (F5/F6, "the single most important test in Suite F") was NOT live-tested** — mainnet has been past both forks for a long time, so no real light-client data spanning that boundary is fetchable any more, and this pass did not build a synthetic two-fork-row fixture either; the `N-FORK` code path (two independent `forks.lookup_row` calls, by `epoch(fin_slot)` and `epoch(t_slot)` respectively) is implemented and reviewable in `anchor_historical` but genuinely unverified by any test in this pass — a real, named gap, not a silently-dropped one. (2) HISTORICAL mode (`anchor_historical`, the `block_roots` fold) compiles and is code-reviewable but was exercised only implicitly (never called by any test in this pass, live or synthetic) — DIRECT mode carries the entire live and structural test burden. (3) `anchor_by_inner_call` (§8.4's secondary hand-off) is implemented nowhere — only the primary log-chain hand-off (`anchor_from_group`) was built and tested, matching the design doc's own §16 candor that the inner-call path "has no live precedent in this codebase." (4) Suite B is a partial measurement (`anchor_direct`, `attest`, both compiled sizes) — `anchor_historical`'s real budget, the isolated `anchor_from_group`/`mpt7_result_against_anchor` cost, and `hash_tree_root_beacon_block_header`'s isolated cost (B5) were not separately measured. (5) O-M8-1 (the paired-fold optimisation) was not implemented; the shipped folds are three independent depth-9 calls as in Candidate B's baseline. (6) The three pre-existing `tests/sync_committee/test_live_e2e_finality.py` live tests were deselected in this pass's regression run because real, CURRENT mainnet sync-committee participation now trips a `box read budget (6144) exceeded` error in that file's own (unmodified) `_choose_mode_and_boxes` helper — confirmed to be a live-data-driven fragility in existing M4 test infrastructure, not anything this pass touched (`contracts/sync_committee/*.py` was not modified); this pass's OWN live test hit the identical failure in the same helper (reused, not reimplemented) and worked around it with padded box references, documented in `test_live_e2e.py`. **§17, gap-closing pass, 2026-08-06 (same session, continued): gaps (1) and (2) above are now CLOSED with real data, plus the human's explicit follow-on ask (a real receipt verified against an M8-anchored root, not M8 tested in isolation).** New files: `tests/state_anchor/test_forks.py` (gap 1); `tests/state_anchor/real_beacon_state.py` + `tests/state_anchor/test_live_historical.py` (gap 2 + the combined-chain proof); `.gitignore` gained one entry (`tests/state_anchor/.cache/`, for the large real beacon-API response cache these tests build). `contracts/state_anchor/*.py`/`contracts/receipt/*.py` untouched — same scope boundary as the original pass. **Gap (1) closed** (`test_forks.py`, Suite F6, "the single most important test in Suite F"): the real Deneb/Electra activation epochs (269568/364032) were independently confirmed three ways (two live `/eth/v1/config/spec` nodes plus `eth-clients/mainnet`'s own published config), and `G_BLOCK_ROOTS_BASE_DENEB=37`/`_ELECTRA=69` were independently re-derived by fetching the real `consensus-specs` container definitions and counting fields, not copied from 008 §3.3. A real archive check was attempted before falling back to a synthetic (but real-constant) fixture: `GET /eth/v1/beacon/light_client/updates` for the sync-committee period straddling the boundary DID return real archived light-client headers on `testing.mainnet.beacon-api.nimbus.team` (better retention than expected), but `GET /eth/v2/debug/beacon/states/{slot}` for a full `BeaconState` near that ~15-month-old boundary 404'd on both reachable endpoints (the other two pool members were unreachable entirely) — so the `block_roots` fold itself is a documented synthetic fixture (`synth.build_tree_at_gindex`), while every constant that COULD be real, is. Three tests: N-FORK's two-row lookup selecting the correct (Electra) base for a genuinely Electra-shaped `fin_state_root` while a Deneb-shaped EL row is used for `epoch(t_slot)` (must succeed, record carries `t_slot`/`FLAG_HISTORICAL`); the same call with a `block_roots` branch shaped for the WRONG (Deneb) base (must reject); and a stale fork table missing the Electra row entirely, fed a genuinely Electra-shaped `fin_state_root` (must reject). All 3 pass live. A 4th test cross-checks the hardcoded epochs against a live `/eth/v1/config/spec` at run time (skips if unreachable). **Gap (2) closed** (`real_beacon_state.py` + `test_live_historical.py`): the real, previously-unexercised `block_roots` fold, built end-to-end from REAL current mainnet data (not synthetic), then anchored via a REAL non-simulated `anchor_historical` call. The real Fulu `BeaconState` field count is **38** (Electra's 37 + exactly one new field, `proposer_lookahead`, EIP-7917) — independently confirmed by fetching the real `specs/fulu/beacon-chain.md` source AND cross-checked against a real fetched state's own `len(data.keys())`; 38 still rounds up to 64 leaves (depth 6, same as Electra), so **`g_block_roots_base = 69` for Fulu — IDENTICAL to `test_live_e2e.py`'s own untested `G_BLOCK_ROOTS_BASE_PLACEHOLDER`, now SHOWN correct, not merely assumed**. The full real `BeaconState` hash_tree_root (all 38 fields, including ~2.33M REAL mainnet validators/balances/participation entries, both real sync committees, the real `latest_execution_payload_header`) was computed with a hand-rolled pure-Python merkleizer (`real_beacon_state.merkleize_with_limit`, the standard append-only-list zero-hash-cache algorithm every serious SSZ implementation uses) rather than via `remerkleable`'s own `View` types directly — `remerkleable` (0.1.28, installed) WAS used, but only to cross-validate this algorithm bit-for-bit at small scale first (packed uint64/uint8 lists, fixed Bytes32/uint64 vectors, container lists, an 8-field Validator-shaped container, `Bitlist`/`Bitvector` including the real SSZ delimiter-bit decoding, and edge cases at `n==limit`/a partial final chunk) — constructing millions of `remerkleable` View objects directly was avoided as needlessly slow, not needed for correctness (measured: the full merkleization of ~2.33M real validators ran in **~24s**, plus ~5s to `json.load` a ~956MB real state dump — bigger than the task brief's ~150-300MB estimate, real current mainnet scale). Matched the real beacon node's own `state_root` for that slot BYTE-FOR-BYTE. A real, genuine dead end was hit and documented before falling back: the task's own suggested `GET /eth/v2/beacon/blocks/{T_SLOT}` does NOT carry a precomputed `execution_branch` for an arbitrary historical slot the way a light-client `finality_update`/`bootstrap` response does (only DIRECT mode gets that for free); `GET /eth/v1/beacon/light_client/bootstrap/{T_SLOT's real header root}` was tried and returned a real, confirmed 404 (`"LC bootstrap unavailable"`) on both reachable Nimbus endpoints (bootstrap only serves a small set of retained checkpoints, not an arbitrary historical root) — so the real depth-4 `BeaconBlockBody` fold is built by hand from the block's own real 13 fields (Electra's `execution_requests` included; Fulu does not modify `BeaconBlockBody`, confirmed against the real fetched spec), handling real non-empty `attestations` (real `Bitlist`-packed `aggregation_bits`, decoded from their real SSZ delimiter encoding) and `blob_kzg_commitments`, plus a full real `ExecutionPayload` tree (real `transactions`/`withdrawals` lists, since a plain block-endpoint response doesn't give precomputed roots the way a `LightClientHeader` does) — both independently cross-checked against the real header's own `body_root` before being trusted. **Real, non-simulated `anchor_historical` submitted successfully three separate times against three different real, currently-live checkpoints** (chain moved forward between runs, exactly as expected — T_SLOT is always `fin_slot - 6000`, ~20h back, comfortably inside the 8,192-slot window): most recently fin_slot=14,933,248, t_slot=14,927,248, EL block 25,690,226, real `receipts_root` anchored byte-identical to the real EL block's own; `attest` returns a record with `FLAG_HISTORICAL` set and `beacon_slot == t_slot` (not `fin_slot`), exactly as §6.1 requires. **Real measured budget: `anchor_historical` (HISTORICAL mode) = 5,761** (`simulate`, generous budget) — meaningfully above DIRECT mode's own measured 3,588–3,652 (G1-M8), consistent with the extra depth-19 `block_roots` fold plus a second full `hash_tree_root_beacon_block_header` call HISTORICAL mode pays that DIRECT mode does not; the real non-simulated submission used a 20-donor group (comfortable headroom over the measured 5,761). One real, pre-existing fragility recurred (not new): real current sync-committee participation again tripped `_choose_mode_and_boxes`'s own box-read-budget cap inside `finalized_m4` — this time "(18432)", a DIFFERENT number from `test_live_e2e.py`'s own "(6144)", confirming this really is live-data-dependent, not a fixed constant — worked around the same way (padded box references), this time maximized to the full structural 16-refs-per-2-txn-group cap rather than a fixed `+4`. **The combined M7+M8 chain, the human's explicit follow-on ask, real and passing**: once `anchor_historical` committed, a REAL transaction was selected from the SAME real EL block (dynamically — a hardcoded tx_index only works for one specific block, and this fixture anchors a fresh real block every run; selection requires 1-3 real logs and every real trie-proof node ≤2048B, since `AnchorReceiptProbe` only implements the MODE_INIT/MODE_NEXT raw-arg path, not M7's own box-staging fallback for oversized nodes) and verified via `AnchorReceiptProbe` (`contracts/state_anchor/bench_app.py`, M7's own unmodified receipt-walk subroutines plus M8's `mpt7_result_against_anchor`, compiled fresh with `handoff.ANCHOR_APP_ID` patched to the REAL just-deployed anchor app id per TP-M8-4) against the SAME on-chain anchor from the `anchor_historical` call above — not a freshly-trusted root. Real group: `[donor, TrustedRootAnchor.attest(el_block_number), AnchorReceiptProbe MODE_INIT, AnchorReceiptProbe MODE_AGAINST_ANCHOR]`; real budget measured at **3,478** (`simulate`); real non-simulated 4-transaction group committed; the recovered `(status, tx_type, n_topics, address, data_hash)` matched the real `eth_getBlockReceipts` receipt byte-for-byte (most recently: EL block 25,690,226, tx_index 1, log_index 0, `status=1`, `tx_type=2`, real `keccak256(log.data)` match). This is the actual property TP-M7-2/the human's follow-on ask exist to prove: a real receipt, provably included under a root that itself came from a real, independently-verified beacon-chain HISTORICAL anchor — proven live, not merely code-reviewed. **Full project regression after this pass: 432/432 tests pass** (430 prior + this pass's 2 new tests in `test_live_historical.py`; `test_forks.py`'s 4 tests were already included in the 430 baseline), same 4 pre-existing `tests/sync_committee/test_live_e2e_finality.py` deselections, unrelated to this pass. **Updated "single biggest thing for the human reviewer to look at"**: gaps (1) and (2) are now closed with real data; what remains open is gap (3) (`anchor_by_inner_call`, still unimplemented, unchanged scope) and gaps (5)/(6) (Suite B's remaining unmeasured items, and the pre-existing M4 live-test box-budget fragility) — none of which this pass's brief asked it to close. Not yet committed — awaiting human review. |
| M9 | Off-chain relayer/client | Implemented, live-tested (G2/G3/G4/G5/G6/G7/G8/G9-M9 closed; G1-M9 honestly open, see notes) | [docs/design/009-relayer-client.md](docs/design/009-relayer-client.md), implementation report in this pass's session log | M4, M6, M7, M8 | **Corrected from the original plan's stale dependency (M5, M3) and stale "42-point MSM chunking" premise** — shipped M4 uses an `ec_add` chain on both paths and never calls `ec_multi_scalar_mul` (004 §2.3), and the real `install_chunk` size is 8 members, not 004 §12.4's estimated ~12. **Scoping decision**: M9 is a refactor for four of six concerns (promote `eth_rpc.py`/`eth_beacon_rpc.py`/`trie_proof.py`/`real_ssz.py`/`real_beacon_state.py`, ~1,660 working lines, into a new `relayer/` package), a rewrite for one (`m7_relayer.py` against a generic group planner), and new construction for one (an `eth_getProof` client for M6 — confirmed to not exist anywhere outside the frozen spike script — plus the group planner itself). `service/x402_endpoint/` is NOT subsumed; the dependency inverts (service imports `relayer`). **Headline finding**: derived the closed-form box-reference formula (`refs = max(|distinct boxes|, ⌈Σ full_declared_size / 2048⌉)`, `txns ≥ ⌈refs/8⌉`) that exactly explains both live box-budget failures hit this session (6144 and 18432, k=2 and k=8 key boxes) — M4's current 2-transaction `submit_update` group is structurally incapable of a worst-case real committee update; needs ≥4 transactions padded with `noop_budget()`. Also: M7's budget convention can drop from 8 filler NoOps to an external donor, freeing 7/16 transaction slots and closing 008 §9.3's flagged T2 cache-miss blocker (17→10 transactions) with no contract change. Every M9-flagged item from 004 §12.4, 006 §13.3, 007 §8.2, 008 §9.4/§15.3 answered in an explicit table (§15.3). **Implementation pass, same day: the `relayer/` package is real and built exactly per §17's file layout** (`sources/`, `codec/`, `ssz/`, `proofs/`, `group/`, `drivers/`, `client.py`, `cli.py`, `config.py`, `errors.py` — 3,742 lines) — G8-M9's import-graph purity rule (no `tests.*`/`algopy`/`fastapi`/`x402`/`pytest` anywhere in `relayer/`; no `algosdk` in `sources`/`codec`/`ssz`/`proofs`) is enforced by a REAL AST-based test (`tests/relayer/test_security.py`), not a comment. `plan_box_refs` (`relayer/group/boxes.py`) implements §7.4's exact closed form with no fixed constant, verified against the real k=1..8 table AND against the two real numbers this session's live tests actually hit (6144/k=2, 18432/k=8) — P-2/P-4, `tests/relayer/test_plan_boxes.py`. **Suite BX (§13.4) genuinely closed both of 004 §16.5's flagged precision gaps by real live measurement, not left as comments**: a real, non-simulated-avoidance `allow_unnamed_resources=False` group that both WRITES (install_open_keys/session, 49,576 B) and READS (a real subgroup-valid `install_chunk` touch of the same just-created boxes, 6,568 B) in ONE atomic group succeeded at exactly 26 refs (the write's 25 + 2, not the 28 an additive hypothesis would need) — BX-1 resolves to **one shared pool, deduped by distinct box name**, not two separate pools. BX-2 (a second transaction touching the same already-referenced box, with ZERO box refs of its own) also succeeded — the full-declared-size charge really is **once per box name per group, not once per touching transaction**. BX-3 reconfirmed duplicate refs each count (2/3 refs measured genuinely insufficient, 4 sufficient, matching `test_install_live.py`'s own prior finding exactly). Net effect: `plan_box_refs`'s `max(len(distinct), ⌈bytes/2048⌉)` formula is not merely "safe under either reading" as §7.4 worried — it is now **measured to be exactly correct**, neither wasteful nor risky. A genuine, real methodology trap was hit and documented while building Suite BX: `SyncCommitteeLiveHarness.call_group`'s hardcoded `allow_unnamed_resources=True` silently defeats ref-count experiments entirely (algod auto-grants missing box access) — Suite BX had to bypass it with a hand-built `AtomicTransactionComposer`/`SimulateRequest(allow_unnamed_resources=False)` call, exactly the trap §7.7 warns the REAL submission path about. **G3-M9 passes exactly**: `relayer.proofs.account.segment_account_proof` reproduces 006 §6.5's real 5-segment USDT/Binance-8 split byte-for-byte (1,596/1,596/540 phase-A + 1,596/1,738 phase-B) against the pinned fixture (P-5). **A real regression was found and fixed inside this same pass** (not shipped broken): `relayer/sources/pool.py`'s first `EndpointPool.get_json` draft required a `dict` response, silently breaking `fetch_updates` (whose real response is a JSON **list**) — caught by this pass's own full-suite regression run (3 live tests erroring), fixed, and reverified; final full-suite run is clean. **Real, first-ever live M6 client fetch (L-5)**: a genuine current-block `eth_getProof` for USDT/Binance-8 (no pinned fixture) reproduced the IDENTICAL 5-segment shape as the historical fixture, live. **Honest gaps, stated plainly, not silently cut**: (1) `EthAvmClient.sync()`/`.anchor()` are real but thin — they read state and decide DIRECT/HISTORICAL/install-phase correctly, but do not yet drive the full real multi-transaction submission loop (that loop, `relayer.group.submit.run`, is real, tested standalone via L-9/L-10 and via M7's own real T1 submission path in `EthAvmClient.prove_receipt`, just not yet wired through `sync`/`anchor` end to end) — completing this was out of this pass's time budget after the box-budget derivation and the rest of the package. (2) `prove_receipt(against_anchor=True)` (M7+M8 combined) raises `NotImplementedError` rather than a real submission — refuses loudly per §11/S-1 rather than silently falling back to the untrusted RPC-rooted path; the real mechanism it would drive is already live-proven directly in `tests/state_anchor/test_live_historical.py`, just not yet reachable through `EthAvmClient`. (3) M7 **T2** (box-staged) has real planning (`relayer/drivers/m7_receipt.py::plan_receipt_calls_t2`) but no wired submission. (4) Suite L (§13.3) ran and PASSED 4 real live tests (L-5, L-9 ×2, L-10) against real endpoints/algod; **L-1/L-2/L-3/L-4/L-6/L-7/L-8 are real test functions, deliberately `pytest.skip`-marked with an explicit reason (not silently absent)** — each needs a fresh, multi-minute-to-multi-hour on-chain orchestration (a full 512-member install is 64 real submissions) that this pass's remaining time did not cover; G1-M9/G2-M9/G4-M9/G5-M9/G6-M9 (the gates these tests would prove) are therefore NOT closed this pass, unlike G3-M9/G7-M9/G8-M9/G9-M9 which are. **G9-M9 passed**: `service/x402_endpoint/` now contains only `main.py` (rewritten to import `EthAvmClient` from `relayer`), `.env.example`, `vercel.json`, `requirements.txt` (`test_client_payment.py`/`.secrets`/`.vercelignore` predate M9 and are out of its promoted-file scope, left as-is) — the four promoted `.py` files are deleted, not duplicated; the live x402 route itself was NOT re-verified this pass (would need a real payment flow, out of time budget) — a named gap, not a silent one. New root-level `pyproject.toml` (minimal, `relayer`-package-only) was added so `requirements.txt`'s new `-e ../..` editable install actually resolves. Full project regression after this pass: pre-existing suite unchanged and passing (432/432, matching the prior baseline exactly — see this pass's own real fix to `pool.py` above), plus 75 new `tests/relayer/` tests (68 passed, 7 honestly-skipped Suite-L items). Two small new real fixtures committed for Suite R's offline tier: `tests/fixtures/relayer/finality_update_fulu.json`/`bootstrap_fulu.json` (captured fresh from real mainnet light-client endpoints this pass, ~65 KB total). | 2026-08-06 | **Follow-up pass, same day, session continued: `EthAvmClient.sync()`/`.anchor()`/`.prove_receipt(against_anchor=True)` now drive REAL multi-transaction submission loops end to end, and 6 of Suite L's 7 previously-`pytest.skip`-marked tests now run live and PASS (no `@pytest.mark.skip` decorator remains on any of the 7; the 7th is a genuine, real, measured dead end, not a shelved inconvenience — see below).** `relayer/client.py` gained `_drive_m4_install`/`_drive_m4_update`/`_submit_update_group` (M4), `_submit_anchor` (M8, DIRECT+HISTORICAL), `_submit_t2_receipt` (M7 T2, real box-staging submission), `_deploy_anchor_receipt_probe`/`_submit_receipt_against_anchor` (the real combined M7+M8 chain, compiling a fresh `AnchorReceiptProbe` with `handoff.ANCHOR_APP_ID` patched to the real configured `m8_app_id` each call, mirroring `TestG3CombinedM7M8ReceiptProof` generically instead of copy-pasting it). **G2-M9 CLOSED**: `EthAvmClient.sync()` drove FOUR separate fresh 512-member `SyncCommitteeVerifier` instances through real bootstrap → 64× `install_chunk` → `install_finalize` → `submit_update` end to end this pass (real fresh app ids 106387/119384/128291/135055 across separate live runs), each landing `cur_gen=1` with on-chain `fin_slot`/`fin_root`/`fin_state_root` matching the real fetched update exactly (test_l1). **G5-M9 CLOSED**: M7 T2 driven with a `DonorIssuer` sibling instead of 8 filler NoOps committed in **7 transactions** across three separate real live runs (real leaf sizes 2,428 / 2,799 / 3,014 / 3,503 B, real EL blocks, `rstatus=R_INCLUDED` each time) — comfortably under the ≤10 target, down from the old convention's 17 (test_l6). **G6-M9 CLOSED**: a real transaction (EL block 25,691,951, tx_index 0, log_index 0) verified against the SAME on-chain M8 HISTORICAL anchor from this pass's own `anchor()` call — recovered `(address, n_topics, data_hash, status, tx_type)` matched the real `eth_getBlockReceipts` receipt byte-for-byte (test_l4). **New real measured numbers this pass** (per `ARCHITECTURE.md`'s citation rule, each from a real non-simulated `send` or a real `simulate` immediately preceding one): `anchor_direct` **3,619** (design doc §12 projected 3,588–3,652 — essentially exact); `anchor_historical` **5,792** (§12 projected ~5,761 — essentially exact); `submit_update` complement mode at real 511/512 participation **91,502** (§12's own 004 §2.4 citation: ~91,384 — essentially exact); `submit_update` DIRECT mode at the SAME 511/512 participation **210,381–211,502** (004 §2.4's own citation: 211,266 — essentially exact, confirmed three independent ways: a standalone probe script, inside `test_l2`'s own sizing attempt, and by cross-referencing the design doc's own historical number). MUST #8 (M8's `N6` ⇒ `RETRY_REPLANNED`, never a bare exception, then resync-and-retry) was proven live for real TWICE, not merely coded: once as `test_l8`'s own explicit property (a real stale-checkpoint `anchor_direct` genuinely rejected on-chain, reclassified, retried, and the retry committed), and once *incidentally*, when a real ~2-minute gap between a live HISTORICAL anchor's own fixture-build (the ~956 MB `BeaconState` fetch + merkleize) and a subsequent DIRECT call let a real additional mainnet finalization land in between — exactly 008 §12.4's "normal, not exceptional" DIRECT race, reproduced by real wall-clock timing, not staged. **Six real, live, non-hypothetical bugs were found and fixed this pass** (each confirmed via a real algod rejection, a real `simulate` trace, or a real cross-run reproduction — none inferred from reading code alone): (1) **`MaxAppTotalTxnReferences`**: a `DonorIssuer` transaction's own `foreign_apps` reference counts against the SAME per-transaction reference budget as its box refs — a real, structural algod rejection ("tx references exceed MaxAppTotalTxnReferences = 8") distinct from and *broader than* §7.3's own box-specific "max number of box references is 8" citation; fixed by capping the donor's own padding capacity at 7 boxes, not 8, and giving `submit_update`/`noop_budget` fillers (no `foreign_apps` of their own) the full 8 (`EthAvmClient._submit_update_group`). (2) T2's box name must be **exactly 8 bytes** (`Mpt7ReceiptApp`'s `MODE_STAGE_OPEN`/`WRITE`/`WALK` all assert `fixed.length == 10`, i.e. name(8)+len(2)) — the client's original box-name construction was 15 bytes; a real, reproduced `assert failed` on-chain before the fix. (3) **`prev_gi`/`walk_index_pointer` absolute-vs-relative bug**: `plan_receipt_calls`/`plan_receipt_calls_t2` computed these as indices local to their own `calls` list, silently wrong the moment a `DonorIssuer` (or anything else) precedes the walk in the real group — `mpt7_state_from_prev` would read the WRONG transaction's log. Fixed with an explicit `group_offset` parameter threaded through all three real call sites (T1, T2, and the combined-chain walk); caught before it ever reached a live submission, by re-deriving `mpt7_state_from_prev`'s own "`gi` is the ABSOLUTE group index" contract from `contracts/receipt/handoff.py` directly. (4) **`N-ADMIT` ring-admission ordering**: with a tiny `ring_n=8` (this pass's own test scale), anchoring DIRECT (the current block) before HISTORICAL (~20h/~6,000-slots-and-thus-thousands-of-EL-blocks earlier) makes the historical block permanently inadmissible (`candidate_block + ring_n <= hi_block`) — a real, reproduced `assert failed` on `contracts/state_anchor/box.py`'s own `ring_admit_and_write`, not a bug in the check itself; fixed by anchoring HISTORICAL first (an empty ring's `hi_block==0` admits anything) then DIRECT (a strictly larger, always-admissible block number). (5) **Wall-clock margin too tight**: `assert_not_future_dated`'s original `signature_slot <= slot_now()+1` margin was real-world too strict — a freshly-fetched real mainnet `finality_update`'s own `signature_slot` was observed live 3–4 slots ahead of `slot_now()` computed at check time (ordinary distributed-systems latency/clock skew between this process and a real public beacon-API endpoint, not a malicious or buggy far-future claim); widened to a new `MARGIN_SLOTS = 12` (~144 s) constant, still a hard assert, still rejecting anything genuinely hours/days out. (6) A **real N6 anchor race**, described above. **G1-M9 (the sole gate NOT closed this pass) — a genuine, confirmed, measured dead end, not a shelved inconvenience**: `test_l2` forces whichever mode (DIRECT/COMPLEMENT) touches all 8 real key boxes and attempts a real submission. Real live participation throughout this entire pass's session was **511/512** (exactly 1 real absentee) — COMPLEMENT (the cheap direction) therefore touches only 1 key box, so the ONLY mode touching all 8 today is DIRECT, whose real measured cost (210,381–211,502 opcodes, cited above) exceeds the shared 256-inner-call `DonorIssuer` ceiling's maximum possible extra budget (256 × 682 net yield/call + ~2,800 base ≈ **177,392**, a hard AVM protocol ceiling — no donor count, transaction count, or client-side change can cross it) — confirmed three independent ways (a standalone probe script measuring the real `simulate` response directly, `test_l2`'s own sized-simulate attempt, and cross-checking against the design doc's own historical 211,266 citation). The underlying mechanism this gate exists to prove — real bitfield-driven box-reference arithmetic sizing a 4-transaction group (`[DonorIssuer, noop_budget, noop_budget, submit_update]`) rather than a fixed 2 — **is** exercised for real, by the identical code path, at COMPLEMENT mode, in every one of this pass's `sync(update=True)` calls (91,502 measured, cited above); what remains genuinely unexercised live is specifically the DIRECT/k=8/high-opcode combination, which requires either a day with a moderate, spread-out absentee count (enough absentees to touch all 8 boxes while still costing under ~177k opcodes) or a structural change M10/M11 would have to make (e.g. two atomic groups instead of one, `O-M9-2`) — `test_l2` documents this honestly with a `pytest.skip()` citing the real measured numbers rather than silently forcing a submission that cannot possibly succeed. **Also fixed this pass**: `env_a_anchor`/`env_a_anchored`'s own fixture construction (test-file bugs, not `relayer/` bugs) — `client.config.m7_app_id` is genuinely not needed for `prove_receipt(against_anchor=True)` (it deploys its own `AnchorReceiptProbe`, never the configured `m7_app_id`) but `prove_receipt`'s guard clause required it unconditionally; relaxed to only require `m7_app_id` on the non-anchor path. **Full project regression after this pass: 501 passed, 1 skipped (`test_l2`, honest, see above), 4 deselected (pre-existing `test_live_e2e_finality.py`, unrelated to this pass), 1 error** — `tests/state_anchor/test_live_e2e.py::TestG1M8RealDirectAnchor` hit the SAME well-documented, pre-existing, live-data-dependent `box read budget (N) exceeded` fragility this project's own standing convention already names (that legacy test file's own `_choose_mode_and_boxes` helper, not anything `relayer/` touches) — confirmed transient by re-running in isolation (identical failure, `N=18432`, real current participation having shifted since the main run). **Honest gaps still open, stated plainly**: (a) G1-M9, above. (b) Resuming an M4 install session stuck in `VALIDATED`/`OPENING_BOXES` (as opposed to `INSTALLING`) is not reachable via this pass's own bootstrap+box-opening atomic grouping and raises `NotImplementedError` — only reachable via the separate `install_begin` rollover flow, out of this pass's added scope. (c) A T2 (box-staged) receipt combined with an M8 anchor has no deployed contract anywhere in this codebase to drive (`AnchorReceiptProbe` only implements the T1 MODE_INIT/MODE_NEXT raw-arg path by design, per `test_live_historical.py`'s own docstring) — a real, contract-level gap, not a client one; `prove_receipt(against_anchor=True)` refuses it loudly with `TierUnsupported` rather than silently downgrading. | 2026-08-06 | **Not committed — awaiting human review**, per this project's own standing rule. G1-M9 is the one gate a future session could still close, either by waiting for a real day with moderate spread-out absenteeism or by building the two-atomic-group fallback (`O-M9-2`) 008 §9.3 already names. Gaps (b)/(c) above are real, named, and small enough that M10 (deployment tooling) or a future M7 revision can absorb them without re-opening this pass's own scope. |
| M10 | Deployment & box-storage schema tooling | Implemented, live-tested (G1 through G9-M10 closed live; G10-M10 satisfied by this row's own cited real responses) | [docs/design/010-deployment-tooling.md](docs/design/010-deployment-tooling.md) | M7, M8, M9 | **Scoping decision**: ~40% promotion of existing ad hoc deploy logic scattered across `tests/*/conftest.py`/`relayer/group/donors.py`/`bench/composer_bench.py` into a new `deploy/` package (imports `relayer`, never the reverse, since `deploy/` needs `algopy`/`puyapy` access G8-M9 forbids `relayer/`), ~60% genuinely new: a generated, versioned box/state schema artifact (produced from the contracts' own `constants.py` by plain import, never hand-written -- already shown to catch 3 real doc-vs-code drifts: `ring_n` vs on-chain `ring_size`, `forks8` 320B not 321B, and the real 9-int/1-byte creator MBR of 406,500 not 378,000); a deployment manifest + converge-by-diff engine (found that deployment already has on-chain cursors for everything except the app id itself, which is separately recoverable by approval-program-hash scan); and a fork-row gindex generator deriving all seven real gindices from `relayer/ssz/` rather than hand-copying them. **Critical live finding**: `Mpt7ReceiptApp`/`MptSegmentApp`/`Mpt6ComposerApp` (raw `Contract`, not `ARC4Contract`) accepted `UpdateApplication`/`DeleteApplication` from anyone -- confirmed by real exploit, already found and fixed in a separate pass same day (see M5/M6/M7 rows) before this doc's approval. A safer two-stage funding recipe (measured, bounds a lost app-id-prediction race to 0.2329-0.3349 ALGO, down from an unbounded ~45 ALGO exposure) and a real `inspect` command (decode any deployment's state/boxes via the schema, auditable by someone who didn't perform the deploy) round out the design. **2026-08-07, Sonnet implementation pass — the mainnet finding, stated first**: this pass's own `deploy inspect`/schema tooling confirms live, against the CURRENT (already-fixed) bytecode, that `SyncCommitteeVerifier` and `TrustedRootAnchor` are `"NoOp only"`-gated and `Mpt6ComposerApp`/`Mpt7ReceiptApp` are now ALSO `"NoOp only"`-gated (each now contains exactly one `OnCompletion` check in its compiled TEAL, `grep -c` confirmed) — the §9.1 `UpdateApplication`/`DeleteApplication` hole this design doc's own approval was contingent on is CLOSED in the code this pass deploys; `deploy apply`'s mainnet-refusal mechanism (`deploy.inspect.refuse_unrestricted_on_mainnet`, tested live in Suite S) is therefore a live-fire-tested safety net with no contract in this repo currently able to trigger it, not a theoretical one. **`deploy/` package + `tests/deploy/` built exactly per §16/§17's file layout** (18 files, ~2,500 lines: `schema/generate.py`, `forks.py`, `create.py`, `mbr.py`, `compile.py`, `diff.py`, `inspect.py`, `manifest.py`, `config.py`, `cli.py`, `plans/{m4,m6,m7,m8,donors}.py`, plus the four `.schema.json` artifacts and `targets/{localnet,testnet,mainnet}.json`). **G8-M10's import-graph rule enforced by a real AST test** (`tests/deploy/test_security_matrix.py`, mirroring G8-M9's pattern exactly): `relayer/` never imports `deploy`; `deploy/` freely imports both `relayer` (donors, `plan_box_refs`, `relayer.ssz`) and `contracts.*`/`algopy` (the schema generator), confirmed non-vacuously both ways. **The schema artifact is generated, not hand-written, and CI-diffed**: `SyncCommitteeVerifier.arc56.json` was compiled and committed this pass (mirroring `TrustedRootAnchor.arc56.json`'s own G8-M8 precedent) so the generator needs zero `puyapy`/algod calls for M4/M8 (byteCode lives inline in the ARC-56 artifact `puyapy` itself emits); `Mpt6ComposerApp`/`Mpt7ReceiptApp` (bare `Contract`, no ARC-56 at all — confirmed by direct `puyapy` compile, no `.arc56.json` is ever emitted for a non-`ARC4Contract` class) instead read a committed `deploy/schema/_compiled/*.compiled.json` cache, refreshed by a separate, explicitly-network-touching `deploy.compile.refresh_bare_contract_cache` function — an honest asymmetry (`deploy/compile.py`'s own docstring), not silently glossed over: `deploy schema --check`/`generate` themselves are 100% network-free for all four contracts, but *regenerating the M6/M7 half of the cache* needs algod, unlike M4/M8's. **Real measured numbers, every one cited to an actual response, none asserted from the design doc alone**: global-state MBR M4 820,500 / M8 406,500 µALGO (ARC-56 schema `(13,7)`/`(9,1)`, confirming §3.3 drift 3's correction exactly); box MBR M4 `forks` 234,900 / `k:` 2,464,500 / `s:` 176,100 / `a:` 44,900, M8 `forks8` 132,900 / `h:` 68,100 / `p:` 80,900 µALGO — all reproduced by `deploy.mbr`'s formula from imported constants alone, then independently confirmed against real `account_info` responses from a real from-scratch live deploy (`amount == min-balance` exactly: M4 334,900, M8 (N=8) 777,700 µALGO). **Compiled sizes**: `SyncCommitteeVerifier` **6,980 B** (never previously recorded in this repo, now is), `TrustedRootAnchor` 3,027 B — both exact ARC-56-`byteCode` matches to this design doc's own citations. `Mpt6ComposerApp` **2,680 B** and `Mpt7ReceiptApp` **3,108 B** — each **4 bytes larger** than this design doc's own §4.6 citation (2,676/3,104), because those citations predate the same-day `OnCompletion` security fix (a real, dated drift this pass found and now encodes correctly, not silently reconciled). **The two-stage funding recipe (§7.1) is real, live, and a real bug was found and fixed in it**: the first implementation funded `get_application_address(I0)` instead of `get_application_address(I0 + 1)` (missing the design doc's own `+1` "one real transaction — the funding Payment itself — intervenes" offset) — reproduced the exact same "balance 0 below min" error the design doc's own probe-recipe footnote describes, fixed, then verified live: a real unfunded-create `simulate` predicted app id and exact MBR with **no signer at all** (`EmptySigner`, `allow_empty_signatures=True`), a real Payment funded exactly that figure, and the real `create` landed at the predicted id with the app account ending funded to the byte (`amount == min-balance`, nothing stranded) — `CreateRaced` (never a retry loop, per §17 item 5) is exercised live too (Suite D's D-11). **The gindex generator (G4-M10) reproduces all seven independently-confirmed real values from `relayer/ssz/` alone** — 169/86/87 (M4 Fulu), 802/803/806/69 (M8 Fulu/Electra), 37 (M8 Deneb's `g_block_roots_base`) — by slicing the SAME already-committed `FULU_FIELDS` list to each fork's real, cited field count (28/37/38) rather than hand-typing a second field-name list, and additionally derives M4's own Deneb-era row (105/54/55), a value that has never appeared in any design doc or test in this project before now. **A real, live, previously-undetected bug was found and fixed via this generator, the highest-leverage kind §1.3 warns about**: an early draft seeded `fulu`'s activation epoch with a placeholder (`500000`) that is GREATER than mainnet's real, already-past `FULU_FORK_EPOCH` (**411392**, confirmed both by `tests/sync_committee/test_live_e2e_finality.py`'s own already-live-verified constant AND by a fresh `/eth/v1/config/spec` fetch against real mainnet this pass performed) — this placeholder would have made every fork-table lookup at the real current epoch silently select the ELECTRA row instead of the FULU one, feeding `submit_update` the wrong `fork_version` and therefore the wrong BLS signing domain. It did not fail silently: it failed loudly and reproducibly as a real `ec_pairing_check ... assert` rejection during Suite E's own live G1-M10 run — a real demonstration, not a hypothetical, of exactly the "wrong gindex is indistinguishable from a right one until something downstream chokes on it" risk §9.2 describes, caught by this pass's own acceptance testing rather than shipped. Fixed by using the same real, already-confirmed constant every other live test in this repo uses. **A full from-scratch live deployment (G1-M10, Suite E) passed**: `deploy apply` created all four contracts + the donor pair from nothing (real dev-mode algod), a `RelayerConfig` built FROM THE MANIFEST ALONE (no hand-edited field) then drove M9's real `sync(install=True)` (64 real `install_chunk` groups) → `sync(update=True)` → `anchor("latest")` → `prove_receipt(against_anchor=True)` against real, live mainnet data end to end — **84.6 seconds wall-clock**, dramatically faster than this design doc's own 15-25 minute estimate because dev-mode algod confirms instantly (no consensus wait), not because any step was skipped. **Idempotency and no-stranded-funds, confirmed live, not just argued**: re-running `apply` against an already-deployed stack sent **zero transactions** (G2-M10, confirmed by an unchanged `algod.status()['last-round']` before/after); every app's `amount - min-balance` was exactly zero except M7's declared T2 float (G8-M10); a resumed deploy killed after `create` but before fork rows appended exactly the missing rows and created no duplicate app (G6-M10); `apply`/`verify`/`plan` all refuse a genesis-hash mismatch (G7-M10) and all three run with **no signer configured** (`plan`, D-10) sending zero transactions. **Suite S's security matrix, live and offline**: the M8-counterparty-hash check refuses to bind a wrong `m4_app_id` before spending any funding Payment; `governance == signer` is refused unless explicitly acknowledged; a tampered manifest (an unrelated app's id substituted for a real one) fails `verify` on the approval-hash pin, not on behaviour; the mainnet-unrestricted refusal mechanism is exercised directly against a synthetic pre-fix schema (since no currently-shipped contract can trigger it for real anymore, stated honestly rather than faked). **Test totals**: 45 new tests across 5 files (13 Suite X schema, 10 Suite G gindex, 11 Suite D live deploy, 10 Suite S security, 1 Suite E end-to-end — all against real dev-mode algod where live is claimed, zero mocks), all passing; project-wide regression re-verified at **501 passed** (pre-existing suite, 1 pre-existing live-network flake in `tests/relayer/test_live_relayer.py::test_l4_receipt_against_anchor_g6_m9` that independently passed on immediate retry in isolation, unrelated to this pass — `deploy/`/`tests/deploy/` never touch `relayer/`, `contracts/**/*.py`, or `service/x402_endpoint/`). **The MBR-recoverability correction (§10.4) is now the tool's own standing claim, not just prose**: `deploy`'s schema/inspect output never describes box MBR as recoverable anywhere (§17 item 15) — every box family's `"deleted_by"` field states its real deleter or `null` plainly. **Honest gaps left open**: (1) no testnet/mainnet run in this pass's own acceptance gate, matching §15 gap 3's own framing — `deploy/targets/testnet.json`/`mainnet.json` exist as real, correctly-shaped skeletons (real genesis hashes, `"deploy": false` for every mainnet contract) but neither was exercised against a live public network this pass; (2) `deploy renounce` is real (submits the actual `renounce()` call after a typed confirmation, never scripted, per `O-M10-4`) but has no automated test, deliberately, since exercising it would permanently renounce a live test deployment; (3) M6 still has no client that submits a real transaction against it (§4.4/§15 gap 4, restated, unchanged by this pass — `deploy` deploys `Mpt6ComposerApp` correctly but nothing in this repo drives it end to end yet); (4) `deploy/targets/testnet.json`'s genesis hash is a well-known, widely-cited public constant, not independently re-fetched by this pass against a live testnet node. | 2026-08-07 | M11 (real-data harness/CI) can now import `deploy.diff.apply`/`deploy.schema.generate` directly for its own from-scratch-deploy fixture instead of hand-rolled conftest helpers; a real testnet run before any mainnet action remains recommended (§6.3, not yet done); M6 still needs a real submitting client before its own deployment is exercised end to end (§4.4/§15 gap 4). |
| M11 | Real-data test harness & CI integration | **Implemented, real CI live and green.** Both workflows real; committed and pushed 2026-08-07. First real `ci-offline.yml` run on a clean GitHub-hosted runner caught two genuine gaps this pass's own local environment had masked: `cramjam` (needed by `tests/sync_committee/vectors.py` to unpack vendored `.ssz_snappy` fixtures) was never declared in `pyproject.toml`'s `test` extra, and the `tests` job's Install step never granted it the `contracts` extra (`puyapy`), so `tests/harness/test_compile_gate.py`'s pytest tests (correctly categorized `offline` — no network, just a local compiler) failed with `No module named puyapy`. Both fixed and verified: run [31227237712](https://github.com/m-reynaldo35/eth-avm-light-client/actions/runs/31227237712) is fully green, 3/3 jobs (`contracts compile + artifact diff`, `offline tier (py3.12)`, `offline tier (py3.13)`) — the first real, test-executing green CI run in this project's history. | [docs/design/011-test-harness-ci.md](docs/design/011-test-harness-ci.md) | M9, M10 | **Headline finding (design pass)**: CI had been running since the scaffold commit and had never once executed a test — 21 historical workflow runs, every one completing in 7-21 seconds executing only the literal placeholder `echo`. **Implemented and measured real, this pass**: `ci-offline.yml`/`ci-live.yml` rewritten from the `echo` scaffold to real bodies; `tests/harness/` (`env.py`/`chain.py`/`deployment.py`/`m4.py`/`tiers.py`/`variance.py`/`quarantine.py`/`report.py`/`compile_check.py`) replaces the 7 algod probes/6 beacon probes/9 URL literals/6 `funded_account`s/cross-package conftest imports §2.2 measured; `_choose_mode_and_boxes`/`_submit_update_group`/`_issue_donor_txn` deleted from `test_live_e2e_finality.py` (asserted permanently absent, Suite H H-5) and its two duplicate happy-path tests removed as `test_live_relayer.py::test_l1`'s own coverage, its two adversarial tests rebased onto `EthAvmClient.submit_update_group` (promoted from private) driven by the new `tests/harness/m4.py` shared fixtures. **Real offline run**: `pytest tests/ --offline --check-tier-manifest` — **500 passed, 0 failed, 92 deselected, 118 s**, real committed `tests/harness/tiers.json` (592 collected: 500 offline / 90 live / 2 live_heavy). **Real live run** (real dev-mode algod, digest-pinned `algorand/algod@sha256:9c667451…`, build 4.7.4/91cbddcd, brought up by the new `.github/scripts/algod_up.sh`, real mainnet beacon/RPC data): every live-tier file run for real except the one `live_heavy` (>1 GB BeaconState) pair, deliberately deferred to the weekly job per its own policy — `tests/bls`+`tests/ssz/test_budget.py` 59 passed/31.6 s; `test_core.py`+`test_box_budget_model.py` 17 passed/28.8 s; `test_live_beacon_fetch.py`+`test_forks.py`+`test_install_live.py` 11 passed/22.2 s; `test_forks_gindex.py`+`test_security_matrix.py` 20 passed/1.0 s; `test_deploy_live.py` 11 passed/273.3 s; `test_live_relayer.py` **6 passed, 1 quarantine-skip** (`test_l2`, G1-M9, via the new committed `quarantine.toml` — no `--deselect`)/207 s; `test_retire_live.py` 4 passed/30 s; `test_end_to_end.py` 1 passed/48 s; `test_live_e2e_finality.py` 2 passed/12 s (the two surviving adversarial tests, no `--deselect`, closing G4-M11); `test_live_e2e.py` 2 passed/16 s (G1-M8 headline, rebased onto the shared harness). **Two new real bugs found and fixed this pass**: (1) `tests/harness/m4.py`'s `installed_committee` depends on `checkpoint_data`/`m4_donor_pair`, which three consumer test files imported without re-exporting, producing a real `fixture not found` error live — fixed by importing all four names explicitly; (2) `python -m deploy.cli schema --check` (the design doc's own §7.4 example) silently no-ops and exits 0 without checking anything, because `deploy/cli.py` has no `__main__` guard — the real entry point is `python -m deploy`; both `ci-offline.yml` and Suite C's C-4 use the corrected form. A committed `quarantine.toml` (90-day-capped expiry, enforced by Suite Q) replaces the tribal-knowledge `--deselect` convention for G1-M9's still-open structural gap. `requires-python` raised `>=3.10` → `>=3.12` (the untested floor is now the real, CI-run one). `requirements.txt`/`pyproject.toml` drift now has a real offline test (F-5, 011 §18 item 17). Every "flagged for M11" hand-off across 002/007/008/009/010 answered explicitly (§15.3), including correcting 002 §9.2's stale `(M11)` owner tags on O-1/O-2 (shipped by M2 itself). | 2026-08-08 | M12 (docs & release): cite this pass's real `ci-offline`/`ci-live` run ids once pushed and a human has reviewed; note in release docs that `test_l2`/G1-M9 remains a live, dated, quarantined gap, not a closed one; py3.12 leg of the matrix untested locally (only 3.13 available in this pass's environment) — confirm on first real push. |
| M12 | Docs & packaging / release prep | Implemented (docs/versioning/packaging); 2 of 9 release-readiness rows still open, both need a human action | [docs/design/012-docs-packaging-release.md](docs/design/012-docs-packaging-release.md) | M10, M11 | **Headline finding**: `ci-live.yml`'s real body (landed in M11) has never executed — all 8 of its scheduled runs before this pass carry `head_sha = 1148ae4`, the placeholder scaffold commit; `ARCHITECTURE.md`'s own rule that `ci-live` "must pass before any module is marked Released" has therefore never once been satisfiable. Manually dispatched for the first time this pass (run in progress at design-approval time). **Two more real corrections found live**: mainnet app `3664247481` (M7/M10 rows' "still live and hijackable") **no longer exists** (confirmed 404, independently re-verified this session after this pass's own deletion); and M7's "not yet done: public HTTPS exposure" is **already done** (`GET /health` on the live Vercel service responds real data). **Versioning scheme**: three axes (AVM/protocol, consensus fork, proof system), identity = the already-CI-diffed approval-program SHA-256 (not a hand-typed number — matches 010 §3.3's own three measured prose-vs-code drifts as the reason to generate, never type); the fork axis is two windows (mutable on-chain table window vs. immutable per-bytecode code window) whose intersection nothing on chain currently computes — a real, previously-unnoticed hazard (`append_fork_row` validates epoch/sentinel/capacity but not gindices at all, so a Gloas row is appendable today and would fail at runtime, not governance time). Real supported fork range corrected: **Deneb/Electra/Fulu**, not `ARCHITECTURE.md`'s guessed "Altair/Capella/Deneb". **Release-readiness checklist**: 20 items, 9 blocking (most a file/flag/two lines of code: missing `relayer/__main__.py` — `python -m relayer` genuinely does not work, confirmed independently; zero committed deploy manifests; README still reads "Early scaffold stage"; 007 §10's four documentation corrections never landed), 2 requiring real runs (a green `ci-live` dispatch, a real testnet deploy — both **required**, mainnet deployment explicitly **not** in scope, M12 writes the runbook only). **Packaging defect, independently reproduced in a clean venv**: `pip install eth-avm-relayer` pulls **59 packages**, 40 of them (`sentry-sdk`, `fastapi-cloud-cli`, etc.) forbidden by `relayer/`'s own G8-M9 import-purity test — only 19 are genuinely needed; fix is moving the three Vercel-only deps to a `service` extra, with a recorded fallback if Vercel's builder doesn't honor extras. Bazaar discovery registration **declined** (nothing monitors the live service); the x402-global-challenge submission tag **sequenced**, not pressed (M12 builds all five preconditions, a human submits); the AlgoPlonk swallowed-error bug (007 §14.3.1) gets reported upstream before the tag, not fixed here. Every "flagged for M12" hand-off across 003/007/008/009/010/011/ARCHITECTURE.md/pyproject.toml hunted down and answered explicitly (§14.3). **G1-M12, real and green, this pass**: after two follow-up dependency fixes (see below), a real, human-triggered `workflow_dispatch` of `ci-live.yml` passed in full -- run [31229821639](https://github.com/m-reynaldo35/eth-avm-light-client/actions/runs/31229821639), `live` 589 passed/1 skipped/2 deselected in 774.5s, `contracts-live` green -- the first real, green `ci-live` run in this project's history, closing the gate `ARCHITECTURE.md` has required since day one but that could never be satisfied before now (§0 of this doc). The FIRST dispatch (run 31228946039, immediately after M11 landed) failed twice for the same root cause pattern as ci-offline's own first run: `algokit` (used by `tests/ssz/test_budget.py` to compile a real TEAL harness for live budget measurement) was never declared in `pyproject.toml`'s `contracts` extra, only working locally because it happened to already be installed via pipx -- fixed (`98d5528`). That same first run also hit a real, non-reproducing N6-shaped live race in `tests/state_anchor/test_live_e2e.py::TestG1M8RealDirectAnchor` (`assert failed pc=1301, frame_dig -1; ==; assert`, i.e. a real finalized-header advance between plan time and submission) -- this specific test uses a raw hand-rolled harness (`Arc4Harness`/`submit_with_donor`) that bypasses `EthAvmClient.anchor()`'s own production `RetryReplanned`-and-resync handling for exactly this failure signature (the same class 009's own G1-M9 pass already documented as "normal, not exceptional"), so it fails hard instead of retrying; deliberately NOT rewritten this pass (M12's own design doc, §1.2 item 7, excludes new test content for M1-M11) -- it did not reproduce on the second, fully green run, consistent with a genuine transient race rather than a persistent bug, and is left as a named, real gap for a future M8/M9-scope pass rather than silently patched over. **2026-08-08, Sonnet implementation pass.** `deploy/versions.json` generated (all seven contracts' `code_id`s filled, including `MptSegmentApp`/`DonorIssuer`/`DonorCallee`, closed this pass by running `deploy.compile.refresh_bare_contract_cache` against a real, reachable algod -- `mainnet-api.algonode.cloud`'s public `/v2/teal/compile`, no signer, no deployment -- the "3 of 7 unfillable offline" gap the design doc named is now closed for real). `deploy resolve --network mainnet --fork fulu --json` and `--fork gloas` both run live against real mainnet with no signer and reproduce the design doc's own worked examples exactly: `m7: USABLE` (app `3665914633`, hash-matched live), `m4`/`m8`: `NOT_DEPLOYED` for `fulu`, `FORK_UNSUPPORTED` (with the cited reason) for `gloas`. `deploy inspect --forks` added, surfacing the two pre-existing `_read_fork_rows` functions. The client-side Gloas refusal (`deploy/versions_guard.py`) is wired into both `deploy/plans/m4.py` and `m8.py`'s `desired_fork_rows`, tested both ways (fires for `gloas`, not for `fulu`). `deploy/manifests/mainnet-v1.0.json` committed, recording M7 + the donor pair with real creation rounds (`63833882`/`63837794`/`63837792`) pulled from the public indexer, verified against real mainnet with no signer (`deploy verify` -> `m7: OK`). `relayer/__main__.py` added -- `python -m relayer` (009 §15.4's own nominated quickstart) now genuinely works, confirmed in a clean venv. `[project.scripts] eth-avm-relayer` added. The `service` extra split landed in `pyproject.toml`; measured in a clean venv, a wheel install now pulls **20 packages** (was 59) -- `sentry-sdk`/`fastapi-cloud-cli`/etc. no longer forced on a library consumer. A named `relayer.errors.MissingContractsSource` replaces the bare `FileNotFoundError` an installed wheel used to hit on `prove_receipt(against_anchor=True)`/`deploy_donor_pair`. `README.md` rewritten (status table, install/quickstart, live-deployment table with a real `deploy verify` command, supported-fork statement, an honest "what this does not do" section, and the 008 §15.6 trust sentence in the same paragraph as the first "verifier"); `docs/security.md`/`versioning.md`/`quickstart.md`/`operating.md`/`release.md` and `CHANGELOG.md` created; `ARCHITECTURE.md`'s two stale sections corrected; 007 §10's four documentation corrections landed in `docs/design/002-rlp-decoder.md` and `docs/design/005-mpt-walker.md`; this row's own two stale claims (public HTTPS exposure, app `3664247481`) corrected above. Suites V/W/N/M added (`tests/harness/test_versions.py`/`test_packaging.py`/`test_doc_claims.py`/`test_manifests.py`, 40 new tests, all passing -- 4 of them live, against real mainnet, no signer needed); two pre-existing tests (`test_compile_gate.py`'s C-3, `test_coverage_discipline.py`'s F-5) updated to reflect the now-5-contract bare cache and the `service` extra split. Local run: `536 passed, 96 deselected` offline, tier manifest clean at 632 collected. **Two of the checklist's nine originally-blocking rows remain open and are explicitly NOT this pass's to close**: G2-M12 (a real, funded testnet deploy-verify-drive) needs a human to provide testnet ALGO; and one real pull request against this repository has still never been observed (0 of this project's workflow runs, ever, have been a `pull_request` event) -- both are human actions, and no `v1.0.0` tag was cut while they remain open. The AlgoPlonk upstream report stays a reviewed-but-unfiled draft (`tests/fixtures/spike-reference/zk-m7/UPSTREAM_ISSUE_ALGOPLONK.md`); the Vercel redeploy-and-verify for the `service` extra could not be attempted (no Vercel CLI credentials in this environment -- `vercel whoami` started an interactive device-login flow rather than confirming a session, per this pass's own instruction not to work around a missing credential). Nothing was committed or pushed by this pass. **Real `ci-offline` green on this pass's implementation push, after three follow-up fixes** (same recurring class as M11's own first-CI-run findings): a missing `build` package (Suite W's `built_wheel` fixture shells out to `python -m build`), and Suite W's W-2/W-6 miscategorized as `offline` when a genuinely clean-venv `pip install` always makes a real PyPI index round trip regardless of local caching -- re-marked `needs_network`, `tiers.json` regenerated (`offline` 536->534, `live` 94->96). Final green run: [31248277269](https://github.com/m-reynaldo35/eth-avm-light-client/actions/runs/31248277269). | 2026-08-08 | Two human actions gate a `v1.0.0` tag: (1) a funded testnet `deploy apply` -> `verify` -> M9 drive (G2-M12, docs/operating.md has the recipe, ≈24-32 ALGO projected), and (2) one real pull request against this repository. Separately, a human with Vercel credentials should redeploy `service/x402_endpoint/` with the new dependency split and confirm `GET /health` still responds for real before treating §4.1's Vercel question as closed either way (option B is recorded in `pyproject.toml`'s own comment if the extra turns out not to be honoured). The AlgoPlonk upstream report is drafted and ready for a human to review and file.  |

## Locked decisions (do not relitigate without a new user conversation)
- Contracts authored in **Algorand Python (Puya)**, dropping to raw `Op`/inline TEAL only in budget-critical inner loops (RLP decode, MSM chunking).
- **Full Ethereum consensus-spec compliance from the start** — no stubbed BLS signature verification, no skipped SSZ track.
- Every module gate: Opus design doc → human approval → Sonnet implementation. No exceptions.
- No cost/budget claim ships without a real `simulate` response behind it (reuse `tests/fixtures/spike-reference/avm_bls_bench.py` / `mpt_bench.py` harness pattern).

## Full plan
See `/home/mark/.claude/plans/peppy-cuddling-snail.md` for the complete module rationale, dependency graph, and execution steps this roadmap was generated from.
