# M9 (docs/design/009-relayer-client.md §2.3): this service now imports
# `relayer` as a library instead of hand-rolling its own RPC/trie/group
# logic -- the four .py modules that used to live in this directory moved
# to relayer/. `-e ../..` installs the repo root in editable mode so
# `import relayer` resolves; a packaged release would pin a real version
# instead.
#
# NOT used by the real Vercel deployment (confirmed live, 2026-08-07):
# Vercel's Python/FastAPI builder resolves dependencies from the repo root's
# `pyproject.toml` exclusively once `[tool.vercel]` is present there --
# this file is read only for local/non-Vercel installs (e.g. `pip install
# -r requirements.txt` in a dev venv). Keep the package list below in sync
# with `pyproject.toml`'s `[project.dependencies]` by hand; nothing enforces
# this automatically yet.
-e ../..
fastapi
uvicorn[standard]
x402-avm[fastapi,avm]
py-algorand-sdk
rlp
pycryptodome
py_ecc
